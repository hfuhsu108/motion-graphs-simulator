# HANDOFF — motion-graphs-sim 階段 2～6（一次做完）

> ## ✅ 本工作單已於 2026-08-21 執行完畢，**已失效，不要再照它動手**
>
> 階段 2～6 全部完成並 commit。現況、實測數值與待辦一律看 `PROJECT_PLAN.md`。
> 本檔只保留作為歷史紀錄。**其中兩處已被推翻**：
> - 第 5 節「車跑出範圍時延展／範圍只增不減」→ 改為**改參數時一次算好整段 0–10 s 的範圍，播放中軸不動**
>   （2026-08-21 使用者裁示，理由見 PROJECT_PLAN 第 3 節）
> - 第 5 節階段 3「曲線畫滿整個 0–10 s」→ 改為**播放中只描每秒一點，t = 10 s 才連成線**
> - 第 4 節「膠囊 `#tangentLegend` 已備好，你只要畫線」仍有效，但**車輛卡內的即時讀數區已整段移除**，
>   改為圖上跟著當前點走的膠囊（x-t 標位置、v-t 標速度）
> - 第 1 節「不做 commit 以外的事」仍有效：**尚未建 GitHub repo、尚未部署**，等真機驗證
>
> 建立日期：2026-08-20｜對象：接手本專案的下一個 session
> 本檔是**一次性接續工作單**，只管「階段 2～6 怎麼做完」。
> 專案的長期權威進度來源仍是 `PROJECT_PLAN.md`；兩者衝突時**以 PROJECT_PLAN 為準**。

---

## 0. 開場：先做這三件事

1. 完整讀 `PROJECT_PLAN.md`。**特別注意第 3 節的「2026-08-20 修訂表」取代了它上方的舊定案**（時間軸改 10 s、不做速度箭頭、直向按稿 C、a-t 預設摺疊）。
2. 讀 `index.html`（1003 行，階段 1 的成果）。第 4 節列出你要動的接點行號。
3. 跑 `git log --oneline` 與 `git status`。本目錄已是獨立 repo（`main` 分支）。

設計稿在 `design_handoff/直線運動模擬器 設計稿.dc.html`（1827 行）。
**你多半不需要開它**——本檔第 5 節已把階段 2～6 要用的數值全部提煉出來。真要開的話：
只有版面定稿 A／B／C（`#b1`/`#b2`/`#b3`）、元件稿 E、佔位頁 F、標註 G 有效；
**難題節 H（`#b8`）與 D 稿的部分說明卡是被定稿推翻的舊方案，照做會做錯**。

---

## 1. 本次任務與節奏（使用者 2026-08-20 指定）

- **範圍**：階段 2、3、4、5、6 全部做完，**停在本機完成**。做 commit；**不建 GitHub repo、不部署**（等使用者真機驗過再說）。
- **節奏**：**連續做完，中途不要停下等確認**。最後交付一份涵蓋階段 2～6 的完整驗證清單，讓使用者一次測完。
- 這是使用者明確給的批量授權，不必每階段回頭問。但**規格有疑義時仍要問**，不要自己發明。

---

## 2. 現況：階段 1 已經做完什麼

版面、互動、狀態機都已完成並驗證過：

- Header／兩欄版面／底部抽屜／底部導覽兩分頁／打點計時器預告頁／三種尺寸響應式
- 車輛卡手風琴：新增（上限 4、禁用而非隱藏）、刪除（剩 1 台禁用）、眼睛顯示隱藏、焦點三重冗餘標示、缺號補位
- 抽屜控制列全部可用：播放／暫停（同一顆）、單步 +0.1 s、重置、速度倍率、每秒自動暫停、時間軸 scrubber
- **時間推進已接上**：`update(dt)` → 游標與車輛卡讀數連動；到 10 s 播放鈕轉「重新播放」態
- 模擬區兩層 canvas 就位；背景層已畫軌道欄分隔線、0 m 基準線、三圖格線與零基線；前景層已畫時間游標
- 物理公式解 `posAt()` / `velAt()` 已驗過三組手算

**還沒畫的東西**（就是你的工作）：車體、每秒軌跡點與秒數、三張圖的曲線、切線、面積填色、座標軸自動延展。

---

## 3. 已定案，不要重問

| 項目 | 定案 |
|---|---|
| 時間軸 | 0–10 s（`T_MAX`），單步 +0.1 s |
| 速度箭頭 | **不做**（垂直 4 欄制下會與軌跡點、秒數打架） |
| a-t 圖 | 預設摺疊成 40px 摘要條，可點開 |
| iPad 直向 | 模擬區在上、設定在下，不做抽屜分頁 |
| 圖表函式庫 | **不引入**（Chart.js 等一律不用），canvas 自繪 |
| 物理 | 公式解，**不做數值積分**、**不累積資料點陣列** |
| 車名與顏色 | A 藍 `#2A6FDB`／B 綠 `#2F9E63`／C 橘 `#E8871E`／D 紫 `#8B5CF6`，與軌道欄位綁死 |
| 焦點車 | 只有焦點車畫切線與面積 |

---

## 4. 程式碼接點（`index.html`，行號為階段 1 完成時的狀態）

```
468  T_MAX / STEP_DT / MAX_CARS / CAR_IDS
474  CAR_COLOR      每車四色：line（曲線與車體）、edge（深一階描邊）、track（軌道深底上的亮階）、tick（秒數標號）
491  TRACK_PAD=3, COL_W=54, COL_CAR=12, COL_DOT=29, COL_SEC=36   ← 軌道 4 欄制的版面常數
494  initState()    state 結構；posRange / vMin / vMax / aRange 就是你要接自動延展的地方
512  posAt(car,t) / velAt(car,t)    公式解，已驗證
517  update(dt)     時間推進與每秒自動暫停；純計算不碰 DOM，維持這個性質
543  measure()      讀出 R.track / R.xt / R.vt / R.at 四個繪圖區（相對 stage-card）
556  tx / yPos / yVel / yAcc        座標映射函式，三張圖共用同一個 tx
579  renderBg()     靜態背景層：只在座標範圍或尺寸變更時重繪
606  render()       動態前景層：每幀重繪
618  ← 「階段 2／3 在這裡接上」的註解就在這行，你的繪製程式碼從這裡長出來
```

**不要動的設計**：`update()` 純計算、`render()` 只讀 state、單一 `state` 物件、兩層 canvas 分工、
`--plot-left:290px` 撐起的三圖對齊（改它會破壞本模擬的核心需求）。

---

## 5. 各階段實作規格

### 階段 2 — 垂直軌道動畫

**車體**：canvas 幾何繪製（圓角矩形車身 ＋ 兩個輪子），扁平填色 `CAR_COLOR[id].track` ＋ 2px 深一階描邊 ＋ 5–6px 圓角。
**禁止在 canvas 內畫 Font Awesome 字符**——那等同 `fillText` 字型字符，有字形載入時機與塑形成本問題（DEV_SPEC 6.3 同一個坑）。FA 只用在 HTML。
車頭朝運動方向（v>0 朝上、v<0 朝下）。焦點車略大（約 20px），其他約 16px。
位置：欄 i 的 `x = R.track.x + TRACK_PAD + COL_W*i + COL_CAR`，`y = yPos(R.track, posAt(car, state.t))`。

**每秒軌跡點**：整數秒 0…floor(t) 的位置**直接代公式算**，不要累積陣列。
`x = 欄起點 + COL_DOT`，7px 圓點；較舊的點用 0.5–0.7 alpha，**最新一顆** 9px ＋ 2px 白框。

**秒數標號**：`x = 欄起點 + COL_SEC`，9px/700 用 `CAR_COLOR[id].tick`；最新一顆 10px/700 白色。
**相鄰兩點距離 < 10px 時省略該秒的數字**（稿明訂，左欄「顯示圖層」卡的說明文字就是講這件事）。

**座標軸自動延展**：車跑出範圍時**不彈對話框、不停止**。
級距用 **1 / 2 / 5 × 10ⁿ** 跳一階，**250 ms 補間**平移縮放，刻度數字淡出淡入而非瞬間跳字。
**範圍只增不減**（同一次播放內不縮回，避免來回抽動）。
**軌道與 x-t 的縱軸永遠同一級距**——它們共用 `yPos()`，改 `state.posRange` 兩邊會一起變，這是對的。
延展時記得同步更新 `renderAxes()` 產生的 HTML 刻度，並呼叫 `renderBg()` 重畫背景層。

**隱藏的車**（`car.visible === false`）不畫車體、不畫軌跡點、不畫曲線。

**驗證**（手算對照，`t = 3 s`）：

| 輸入 | 預期 |
|---|---|
| x₀=0, v₀=20, a=0 | x = 60 m、v = 20 m/s，軌跡點**等距** |
| x₀=0, v₀=0, a=2 | x = 9 m、v = 6 m/s，軌跡點間距 **1:3:5** |
| x₀=50, v₀=−10, a=2 | x = 29 m、v = −4 m/s，先往負向再折返（t=5 s 時 v=0） |

數值本身階段 1 已驗過（讀數卡走公式解）；**你要驗的是繪製結果**：軌跡點落點、間距比例、車體與 x-t 曲線同高。

### 階段 3 — 三張圖的曲線

**線寬**：焦點車 **3.5px**、其他 **2px**（粗細也是焦點線索，不只靠顏色）。
**a-t 上 a=0 的車會與零基線重疊 → 那條改畫虛線**，避免看起來「線不見了」（稿明訂）。

**曲線畫滿整個 0–10 s，不是只畫到游標**（稿 b1 的三張圖都是整條）。
游標負責指示「現在在哪」，面積填色才只填到游標。
⚠ 這與 DESIGN_BRIEF 開頭那句「看三張圖即時長出來」有張力。**依定稿畫滿**；
若使用者驗收時說想要「慢慢長出來」，改法是把曲線裁到 `state.t`（很小的改動），不要在沒被要求前先做。

**取樣**：v-t 是直線、a-t 是水平線 → 各 2 點即可。**只有 x-t 需要取樣**（拋物線），
每 2–3 px 取一點即可，`plot` 寬約 700px → 約 300 點／條。
**每幀不要 new 陣列**——重用同一組 buffer，或直接在迴圈裡 `lineTo`（DEV_SPEC 6.4）。

**v-t 縱軸自適應**：`state.vMin/vMax` 目前寫死 −20/40，改成依所有可見車在 0–10 s 的 v 值域計算，
同樣走 1/2/5×10ⁿ 級距、只增不減。a-t 同理（`state.aRange`）。

### 階段 4 — 切線與面積（只畫焦點車）

**切線**：在游標處，2px 虛線 `#1C2433`，**下方先壓一條 6px 白色描邊**（確保跨過任何曲線都看得見），
**長度固定 ±4 s，不隨縮放改變**。斜率數值標在**圖左上的膠囊**，不壓在曲線上。
膠囊的 HTML 已經在階段 1 做好：`#tangentLegend` / `#tangentVal`，數值已在 `updateReadouts()` 更新，你只要畫線。

**面積**：v-t 上 0→t 的曲線下方，用**焦點車色 22% 不透明度**平塗，上緣就是曲線本身。
數值膠囊同樣已備好：`#areaLegend` / `#areaVal`。

**曲線上留一個 11px 圓點 ＋ 2px 白框**標記當前時刻（稿 b1 有畫，x-t 與 v-t 各一個）。

**驗證（三者必須完全一致）**：切線斜率數值 ＝ 同時刻 v-t 圖的高度 ＝ 車輛卡的 v 讀數。
面積數值 ＝ `x(t) − x₀`（用階段 2 的手算組核對）。

### 階段 5 — 拖曳回放

scrubber 本身階段 1 已完成（只改 `state.t`、拖曳時自動暫停、放手不自動續播）。
本階段要確認的是**繪製端跟得上**：拖曳時軌跡點要正確地增減（因為軌跡點是由 `floor(t)` 算出來的，天然會對）。

**驗證**：播放中拖曳、暫停中拖曳、拖到 0、拖到 10 s 末端、快速來回拖；放開後接續播放的行為正確。

### 階段 6 — 收尾

佔位頁與響應式階段 1 已完成，本階段主要是**跑 DEV_SPEC 第 8 節「交付前檢核清單」逐項回報 ✅/❌**。
外加：刪掉任何 `console.log`／TODO／沒用到的函式；確認 `file://` 直開仍正常。

---

## 6. 驗證方法與環境限制（很重要，別浪費時間）

本專案用 Claude Browser pane 驗證，`preview_start {name:"motion"}`（port 4175，serve root 已限本目錄）。
**npx 冷啟動約 10 秒**，起完先輪詢等 HTTP 200 再驗，不要 curl 一次就以為失敗。

**這個環境驗不了的事**（別硬試，直接列進交付說明請使用者實測）：

- **rAF 不執行**（頁面處於 hidden），所以「真的按播放跑動畫」驗不到。
  替代做法：直接呼叫 `window.update(dt)` 再 `render()`，用迴圈推進，驗邏輯與繪製結果。
- **`file://` 雙擊直開**：工具把專案外的 file:// 渲染成靜態快照。
  替代做法：靜態證據（無 `fetch`／`XMLHttpRequest`、無 `/` 開頭絕對路徑、唯一外部資源是 FA CDN）。
- **4× CPU throttling 的 fps**：量不到，列給使用者。
- **全螢幕（Fullscreen API）**：pane 的 iframe 會拒絕 `requestFullscreen`，但
  `document.fullscreenEnabled` 仍回報 `true`。點下去之後 `fullscreenElement` 還是 `null`，
  此時任何「已退出全螢幕」的斷言都是**假綠燈**（沒進去過，當然不在全螢幕）。
  替代做法：攔截四個 exit API 記錄呼叫序列、偽造 `webkitIsFullScreen` 與 `window.innerHeight`，
  驗分支選擇邏輯而不是驗端對端行為。
- **教室大屏的瀏覽器行為**：完全無法在開發端模擬。**筆電與 iPad 通過不代表大屏通過**——
  2026-08-26 的全螢幕退出 bug 只在大屏出現（見 8.14 D8）。涉及全螢幕、DPR、投影顯示的改動，
  一律在交付說明把大屏標成「待實測」。三種裝置的分工見專案 memory `sim-test-devices`。

**能驗而且該驗的**（用 `javascript_tool` 做機器斷言，不要只看截圖）：

```js
// 三圖左右邊緣嚴格對齊——本模擬的核心需求，每次改版面都要重驗
const r = s => { const b = document.querySelector(s).getBoundingClientRect(); return [b.left, b.right]; };
const [xt, vt] = [r('#plotXt'), r('#plotVt')];
xt[0] === vt[0] && xt[1] === vt[1]   // a-t 展開時三者都要相等
```

截圖可用但**面板未顯示時會逾時**，逾時就改走 DOM 斷言，不要反覆重試。
另有一個已知假象：**頁面載入當下 `devicePixelRatio` 報 1，稍後才變真值**，
所以「載入後立刻讀 `canvas.width`」會看到 CSS 尺寸而誤判 `fitCanvas()` 壞了——
判別法是手動再呼叫一次 `fitAll()`。詳見 `handbook/pitfalls-services.md` 第 3、12 條。

---

## 7. 交付時要給使用者什麼

1. **一份涵蓋階段 2～6 的完整驗證清單**（使用者要一次測完，不要分散在各階段）
2. **DEV_SPEC 第 8 節檢核清單逐項 ✅/❌**，做不到的寫明原因，不要含糊帶過
3. **本次刻意沒做**的清單（限制＋理由＋解除條件）——包含上面第 6 節那三項環境驗不了、要使用者實測的事
4. 手算對照表的實際數值，證明物理正確
5. commit 完成，但**不要建 GitHub repo、不要 push**（使用者指定停在本機）
6. 回頭更新 `PROJECT_PLAN.md` 的狀態表與階段標記；本檔（HANDOFF.md）在階段 6 完成後就失效，可以刪掉或標記為已完成
