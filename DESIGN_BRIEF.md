# Design Brief — 直線運動模擬器（等速度／等加速度）

> 給 Claude Design 的外觀設計委託書。本文自包含，你不需要看過其他對話。
> 建立日期：2026-08-18

---

## 0. 一句話需求

為台灣國中理化課設計一個網頁互動模擬的**介面外觀**：學生調整車子的「初始位置、初速度、加速度」三個參數，
按播放後看車子在直線軌道上跑，同時看 **x-t、v-t、a-t 三張圖即時長出來**。

我要的是版面稿與元件稿，不是程式碼。

---

## 1. 專案脈絡

| 項目 | 內容 |
|---|---|
| 使用者 | 台灣國中學生（13–15 歲）與理化教師 |
| 使用情境 | 教師課堂投影講解 ＋ 學生自己在裝置上操作探索 |
| 裝置 | Windows 桌機（1366×768 為主）、iPad 橫向（1024×768）、iPad 直向（768×1024）。**觸控是主要輸入方式之一** |
| 技術現實 | 成品是**單一 HTML 檔**，CSS/JS 內嵌，無框架、無 build step，部署於 GitHub Pages |
| 系列一致性 | 這是一個模擬系列（浮力、電解質、遺傳…）中的一支，**必須看起來是同一系列**，已有共用設計規範（見第 2 節） |
| 風格定位 | 仿 PhET 的教學模擬介面。**資訊密度優先，不做裝飾性 UI**；功能正常比花俏重要 |

### 這個模擬要教什麼（理解教學意圖才設計得對）

國中生看得懂「車在跑」，但看不懂「x-t 圖的斜率就是速度」。本模擬的**全部價值**在於讓三張圖與
車的運動在同一個時間軸上對齊，學生才能親眼看到：

- x-t 圖變陡的同時，v-t 圖的高度在上升
- v-t 圖曲線下的面積，就是 x-t 圖上同一段時間的高度落差
- a-t 圖是一條水平線（等加速度），且它決定了 v-t 的斜率

**所以「三張圖上下嚴格對齊、共用同一條時間軸」是本設計不可妥協的核心。**

---

## 2. 既有設計規範（必須遵守）

以下是本系列共用規範的完整摘要。**請直接沿用，不要另創色票或字體。**

### 2.1 版面骨架（三欄 ＋ 底部抽屜）

```
<body>  高度 100vh，flex column，背景 #F6F7F9
├─ Header        h-50px 白底、下框線 #E2E5EA：Logo(28px 方塊) + 標題(17px/700) + 右側「?、全螢幕」圖示鈕(34px)
├─ <main>        flex-1，三欄 grid-cols-[1fr_2fr_1fr] gap-3 px-3.5 pt-3
│   ├─ 左欄 設定    白卡：滑桿、膠囊、核取、下拉
│   ├─ 中欄 模擬畫布  圓角 10px，動畫在此
│   └─ 右欄 數據    數據卡直排，常駐顯示
├─ 底部抽屜      白卡 rounded-xl，把手 44×5px：播放/暫停/單步/重置、速度滑桿
└─ <nav>         h-56px 深色 #1E2836：模擬內分頁（選中 #2A6FDB 實底）
</body>
```

**響應式斷點 900px**：寬 < 900px（涵蓋所有 iPad 直向與分割畫面）時，左右欄摺入底部抽屜、
變成「設定 / 數據」兩個分頁，畫布佔滿中間。
**不要用 768px 當斷點** —— iPad 直向寬度就是 768 起跳，用 768 會讓摺疊版面在真機上永遠不觸發。

### 2.2 介面色彩（介面只用這一組；彩色留給畫布）

| 用途 | 色碼 | 說明 |
|---|---|---|
| 主色 Primary | `#2A6FDB` | 按鈕、選中、滑桿、進度 |
| 主色淡底 | `#E8F0FD` | 選中底、提示卡（框 `#C4D7F5`） |
| 深色 Nav | `#1E2836` | 底部導覽列（未選文字 `#9DAABB`） |
| 頁面底色 | `#F6F7F9` | body 背景 |
| 卡片 Surface | `#FFFFFF` | 邊框 `#E2E5EA`；控制邊框 `#CFD5DD` |
| 文字三階 | `#1C2433` / `#445062` / `#7A8496` | 標題 / 標籤 / 輔助 |
| 成功 | `#2F9E63` | 答對、穩定狀態 |
| 警示 | `#D64545` | 答錯、危險操作 |

### 2.3 畫布繪圖配色（本模擬會用到的部分）

- **速度／運動方向向量**：`#E8871E`（橘）— 3px 桿 ＋ 實心三角頭，起點在物體幾何中心，長度正比於大小
- **圖表序列色（多條曲線時依序取用）**：藍 `#2A6FDB` → 綠 `#2F9E63` → 橘 `#E8871E` → 紫 `#8B5CF6`
- **座標軸**：`#CFD5DD` 2px
- **儀器繪圖風格**：扁平填色 ＋ 2px 深一階描邊 ＋ 6px 圓角，**不做擬真漸層陰影**

### 2.4 字體

```css
font-family: system-ui, -apple-system, "Segoe UI", "PingFang TC", "Microsoft JhengHei", sans-serif;
```

- **不載入任何網路字型**（Windows 落在微軟正黑、iPad 落在蘋方）。這是硬規定，理由見第 6 節。
- **字重只用 400 / 700**（微軟正黑沒有 500/600，會合成假粗體，兩平台粗細不一致）
- 所有數值容器一律 `font-variant-numeric: tabular-nums`（等寬數字，數值跳動時版面不位移）
- 級距：標題列 17px/700 · 數據大字 22–24px/700 · 控制標籤 13–14px/700 · 輔助 12–13px/400 · **最小 12px**
- `<input>` / `<select>` 的 font-size ≥ **16px**（iOS Safari 對更小的輸入框會在聚焦時自動放大整頁）
- 固定高度元件內的文字用 **flex 置中**，不要用「line-height = 容器高」置中（兩平台 baseline 不同會各偏一邊）

### 2.5 共用元件

- **按鈕**：主要 = 主色實底 h-44 rounded-10px 白字 700；次要 = 白底框 `#CFD5DD`；圓形圖示鈕 44px
- **選項膠囊**（2–4 個短選項多選一）：h-40 全圓角；選中主色實底白字，未選白底灰框
- **滑桿**：軌 6–7px `#D3D9E1`、已填主色、把手白底主色框 22–26px；標籤列「名稱 … 數值+單位」置於上方，兩端標 min/max
- **Toggle switch**（即時生效的雙態）：52×30px，開 `#2A6FDB`、關 `#CFD5DD`，白色圓鈕 24px
- **核取方塊**（顯示層開關）：22px rounded-md，勾選主色實底白勾
- **數據卡**：白卡，標籤 12px `#7A8496` ＋ 數值 22–24px/700 ＋ 單位 12px
- **底部導覽分頁**：h-38px rounded-lg，選中主色實底
- **圓角**：卡片 10–14px、按鈕 8–10px、膠囊全圓。**間距節奏 8 / 12 / 16 / 26px**
- **陰影只用於抽屜與滑桿把手**，其餘用 1px 邊框分層

### 2.6 觸控

- **所有觸控目標 ≥ 44×44px**。滑桿把手視覺 22–26px，但感應範圍要放大到 44px
- hover 樣式必須包在 `@media (hover: hover)`，否則 iPad 點一下會殘留 hover 不消失
- **資訊不得只靠 hover 呈現**（tooltip 一律改為點擊切換）

### 2.7 圖示

UI 圖示一律 **Font Awesome 6**（cdnjs，本專案唯一允許的外部資源）。
**禁止用 emoji 當 UI 圖示，禁止裸用 `▶ ⏸ ↺ ⛶` 等幾何符號** —— iOS 會把它們渲染成彩色 emoji，變色變形。

---

## 3. 建議一併提供給你的檔案

以下四個檔案在**上一層的 `_shared/` 資料夾**（本系列所有模擬共用的規範來源），
請一併開來看（前兩個是規範原文，後兩個能讓你直接看到本系列既有長相）：

| 檔案 | 內容 |
|---|---|
| `../_shared/design/DESIGN_SPEC.md` | 視覺規範原文（第 2 節即其摘要） |
| `../_shared/DEV_SPEC.md` | 工程規範（字體細則、觸控、效能） |
| `../_shared/design/layout.html` | **版面範例**（三欄＋底部抽屜），瀏覽器直接開得起來 |
| `../_shared/design/components-spec.html` | **元件視覺總覽**（按鈕、滑桿、膠囊、數據卡的實際長相） |

---

## 4. 本模擬的資訊架構與內容清單

底部導覽列有**兩個分頁**：

- **分頁 1「車子」** — 本次設計的主體，以下 4.1–4.5 全部屬於這個分頁
- **分頁 2「打點計時器」** — 功能尚未開發，本次只需要一個「開發中」佔位頁（見 4.6）

### 4.1 左欄：車輛設定

- 預設只有 **1 台車**，呈現為 1 張可摺疊的車輛卡
- 卡片下方有「**＋ 新增車輛**」按鈕，每按一次多一張卡，**上限 4 台**（滿 4 台後按鈕要呈禁用或隱藏）
- 每張卡可**刪除**（第 1 台車也可刪嗎？→ 至少保留 1 台，最後一張卡不給刪）
- 每張卡可**摺疊／展開**
- 同時只有一台是「**焦點車**」（點卡片即選定），焦點車決定右欄數據與圖上的切線／面積畫在誰身上

每張車輛卡內含**三個滑桿**（含數值顯示與單位）：

| 參數 | 介面文字 | 範圍 | 步進 | 單位 |
|---|---|---|---|---|
| 初始位置 | 初始位置 x₀ | −100 ～ 100 | 5 | m |
| 初速度 | 初速度 v₀ | −40 ～ 40 | 1 | m/s |
| 加速度 | 加速度 a | −10 ～ 10 | 0.5 | m/s² |

> 註：下標（x₀ 的 0）在實作時會用 HTML `<sub>` 標籤，**不會用 Unicode 下標字元**。
> 你在稿上照常畫成下標即可。`m/s²` 的上標 2 同理。
> 數值可能是負數且含小數（如 `−7.5`），請預留寬度，負號用 `−`（U+2212）不用連字號。

### 4.2 中欄上半：軌道動畫區

- 一條水平直線軌道，底下有**位置刻度尺**（單位 m，標數字）
- 每台車是一個小車圖示（側視），車體用該車的序列色
- **每秒軌跡點**：車每經過 1 秒在軌道上留一個點，點間距逐次拉開＝等加速度（此為本模擬最重要的視覺說服力）
- **速度向量箭頭**：從車體中心畫出，長度正比於速度大小，變號時反向
- **座標軸會自動延展**：車跑出目前刻度範圍時，刻度尺自動放大範圍（不撞牆、不中斷）。
  請設計成刻度數字改變時視覺不突兀
- 4 台車在**同一條軌道**上（不是四條軌道），可能互相重疊 —— 請想一個重疊時仍分辨得出的畫法

### 4.3 中欄下半：三張堆疊圖（本設計的核心）

**三張圖垂直堆疊，共用同一條時間軸（橫軸 t，0–20 s），左邊緣與右邊緣必須嚴格對齊。**

| 圖 | 縱軸 | 內容 |
|---|---|---|
| x-t | 位置 (m) | 每台車一條曲線（等速為直線、等加速為拋物線） |
| v-t | 速度 (m/s) | 每台車一條直線 |
| a-t | 加速度 (m/s²) | 每台車一條水平線 |

- **一條垂直的時間游標線貫穿三張圖**，位置＝目前時刻，隨動畫移動、也可被拖曳（見 4.5）
- 曲線顏色 = 該車的序列色（與軌道上的車體同色，這是學生對應「哪條線是哪台車」的唯一線索）
- 只有**焦點車**會額外顯示：
  - **x-t 圖上的切線**：在游標處畫一條切線，並標出斜率數值（＝該時刻速度）
  - **v-t 圖上的面積填色**：把 0 到游標時刻的曲線下方填色，並標出面積數值（＝位移）
- 三張圖各需要縱軸標籤（`位置 (m)`、`速度 (m/s)`、`加速度 (m/s²)`），最下面一張才標橫軸 `時間 (s)`

### 4.4 右欄：數據與圖層開關

- **焦點車的四張數據卡**：t（時間, s）、x（位置, m）、v（速度, m/s）、a（加速度, m/s²）
- 卡片標題要標明是「哪一台車」的數據（含該車色標）
- **圖層核取方塊**（四個，控制畫面上顯示什麼）：
  - 每秒軌跡點
  - 速度箭頭
  - x-t 切線（斜率）
  - v-t 面積（位移）
- 另有一個 toggle：**每秒自動暫停**（動畫每跑滿 1 秒自動暫停，讓教師提問）

### 4.5 底部抽屜：播放控制與時間軸

一列控制項，全部觸控目標 ≥ 44px：

- 播放 / 暫停（主色圓鈕 44px）
- 單步
- 重置
- 速度倍率滑桿 0.25× – 2×
- **時間軸 scrubber（本模擬特有，最重要）**：一條 0–20 s 的可拖曳時間軸。
  拖到哪裡，車就退回／前進到該時刻的位置、三張圖的游標線同步移動。
  這是課堂上「停在第 3.5 秒討論」的關鍵操作，**要夠大、夠好拖，且看得出目前時間值**

### 4.6 分頁 2「打點計時器」

功能尚未開發。本次只需要一個佔位頁：可以點進去，內容是一張簡短的說明卡
（例如「打點計時器模擬開發中」＋一句預告）。請設計得像是刻意的預告，不要像壞掉的空白頁。

---

## 5. 需要你解決的五個難題

這五題是委託你的核心，請針對每一題給出明確方案（可附小圖或標註）：

### 難題 1｜iPad 直向（768×1024）時，軌道 ＋ 三張圖怎麼分配高度？

扣掉 Header 50px、底部導覽 56px、底部抽屜，中間可用高度大約只剩 700–750px。
軌道動畫要留約 200px，三張圖平分剩下的約 500px → 每張圖不到 170px（還要扣軸標）。
再考慮左右欄已摺入底部抽屜，抽屜展開時又會再吃掉高度。

**請提出方案**：是三張圖等高？還是 x-t 大、v-t 中、a-t 壓成細長條（因為等加速度的 a-t 就是一條水平線，
資訊量最少）？還是圖表區可垂直捲動？請同時說明桌機（1366×768）時的高度分配。

### 難題 2｜4 張車輛卡全展開時，左欄怎麼捲？焦點車怎麼標？

一張卡有 3 個滑桿加標題，展開約 200px，四張就 800px，左欄放不下。
**請提出方案**：卡片摺疊後的樣子（摺疊時要看得到什麼摘要資訊？）、左欄的捲動處理、
以及「焦點車」的視覺標示法（邊框？左側色條？底色？請避免只靠顏色，色弱學生要能分辨）。

### 難題 3｜抽屜內：5 個播放控制 ＋ 速度滑桿 ＋ 時間軸 scrubber 怎麼排？

全部都要 ≥44px 觸控目標，而抽屜在 iPad 直向時還要塞下摺入的「設定／數據」分頁。
**請提出方案**：一列還是兩列？scrubber 佔滿整寬還是與控制並列？目前時間值顯示在哪？

### 難題 4｜四台車的配色，與「速度箭頭一律用橘色」的規範衝突

規範第 2.3 節規定速度向量一律用橘 `#E8871E`，而圖表序列色第 3 個也是橘 `#E8871E`。
四台車時，第 3 台車就是橘色 —— 若所有車的速度箭頭都用橘色，藍色的第 1 台車配一支橘箭頭，
學生會誤讀成「那是第 3 台車」。

**我的傾向是：速度箭頭改用該車自己的序列色**（箭頭形狀仍照規範：3px 桿 ＋ 實心三角頭），
畫布上加圖例說明。但這是對既有規範的偏離，**請你評估並給建議**（也可能有更好的第三種解法，
例如箭頭用同色但加白色描邊、或車體與曲線改用其他四色組合）。

### 難題 5｜「開發中」佔位頁的樣子

見 4.6。請給一張。

---

## 6. 硬約束（不可違反，違反等於不能用）

1. **不載入任何網路字型**（Google Fonts、Noto Sans TC、自載 woff2 全部不行）。
   理由：離線可用是需求；且過去曾因 Unicode 上下標字元在微軟正黑缺字而 fallback 到別的字型，
   造成大小、粗細、基線全部不搭的跑版事故。只用第 2.4 節的系統字型堆疊。
2. **Font Awesome 6 是唯一允許的外部資源**。圖示只能從 FA6 挑，且要註明用哪個圖示名。
3. **不要輸出 React / Vue / Tailwind 程式碼**。成品是單一 HTML 檔內嵌原生 CSS/JS，
   Tailwind（含 Play CDN）也不能用。你可以用 Tailwind 的**數值語彙**描述間距（如 `gap-3`），
   但不要交付需要 build step 的程式碼。
4. **色票只用第 2.2、2.3 節那一組**，不要新增品牌色或漸層（水面漸層等既有例外除外）。
5. **觸控目標 ≥ 44×44px**；響應式斷點 **900px**（不是 768px）。
6. **字重只用 400 / 700**；字級最小 12px。
7. **不做裝飾性 UI**：無插畫背景、無擬真陰影、無玻璃擬態。資訊密度優先。

---

## 7. 交付格式

**要**：

- 三種尺寸的完整版面稿：**1366×768（Windows 桌機）**、**1024×768（iPad 橫向）**、**768×1024（iPad 直向）**
- 元件狀態稿：車輛卡（展開／摺疊／焦點／非焦點）、滑桿、播放控制列、scrubber、數據卡、核取、底部導覽
- 三張堆疊圖的細部稿（含軸標、格線、時間游標、切線與面積填色的樣子）
- 打點計時器佔位頁
- **色票與間距標註**（用第 2.2 節的色碼，標出你用的間距數值）
- 第 5 節五個難題的**文字說明**（你的方案與理由）

**不要**：

- React / Vue / Tailwind 程式碼，或任何需要 build step 的產出
- 新字型、新色票、裝飾性插畫
- 只在 hover 才出現的資訊

---

## 附錄：參考標的

台灣 CoSci 平台上有一個同主題模擬（`Velocity and motion of uniform acceleration`，EJS 移植版），
它的做法與本設計的差異，可以幫你理解我們要的重點：

| 面向 | 那個模擬 | 本設計 |
|---|---|---|
| 三張圖 | **分頁切換**，一次只看一張 | **垂直堆疊、共用時間軸、游標貫穿** |
| 回放 | 跑完不能回頭，只能重跑 | **時間軸 scrubber 可拖曳回放** |
| 斜率／面積 | 沒有 | **焦點車顯示 x-t 切線與 v-t 面積** |
| 車輛數 | 固定 1 台 | **1–4 台可增減** |
| 邊界 | 撞到端點跳對話框中斷 | **座標軸自動延展，不中斷** |
| 介面 | 英文為主，EJS 預設控制項外觀 | 繁體中文，本系列規範外觀 |

它值得保留的優點是：**參數只有三個**（初始位置、初速、加速度），剛好對應
x = x₀ + v₀t + ½at²，不多不少；以及**每秒軌跡點**把「相等時距、位移逐次變大」講得很清楚。
