# motion-graphs-sim — 專案計畫與接續工作單

> 直線運動模擬器（等速度／等加速度），呈現 x-t、v-t、a-t 三張圖。
> 本檔是**跨 session 的唯一權威進度來源**。接手開發前先完整讀過本檔。

---

## 狀態

| 項目 | 現況 |
|---|---|
| 目前階段 | 「車子」分頁**階段 2～6 完成**（2026-08-21）。「打點計時器」分頁**階段 T1～T6 完成**（2026-08-24，見 8.10），同日完成**第二批五項變更**（見 **8.11**），2026-08-25 完成**第三批六項變更**（旋轉鈕文案、模式膠囊上移、頻率兩處並存、手動拉紙才計時、每條紙帶各自測距、放大檢視可選點，見 **8.12**） |
| 下一步 | **使用者真機驗證（兩個分頁一起）**：iPad Safari／Windows Chrome、`file://` 雙擊直開、4× throttling 的 fps，以及打點頁的三種觸控手勢（拖時間軸、手動拉紙、放大檢視裡點選打點）。通過後才談版號、tag 與 GitHub Pages 部署 |
| 已寫程式碼 | `index.html`（單一檔案、CSS/JS 內嵌，2876 行；打點計時器分頁佔約 830 行）。設計稿在 `design_handoff/`，已由本目錄 `.gitignore` 排除 |
| 待裁示 | **軌跡點在前段被壓縮**的取捨（見第 7 節末列），要不要改成非對稱位置軸由使用者決定 |
| repo | 獨立 repo（`main`），`.gitignore` 排除 `design_handoff/`。**2026-08-25 已推上 GitHub 並開啟 Pages**：`hfuhsu108/motion-graphs-simulator`（公開），線上網址 https://hfuhsu108.github.io/motion-graphs-simulator/ 。**repo 名與資料夾名 `motion-graphs-sim` 刻意不同**，配合既有的 density-simulator／mendel-simulator 命名。不屬於母倉版控（母倉 `.gitignore` 已排除本目錄，已驗證非巢狀） |

---

## 1. 專案定位

- 給台灣國中理化課用的直線運動模擬，教「等速度運動」與「等加速度運動」
- 學生：Windows 桌機（1366×768）＋ iPad（橫 1024×768 / 直 768×1024），觸控是主要輸入之一
- 成品：**單一 `index.html`**，CSS/JS 內嵌，無框架無 build step，GitHub Pages 部署
- 必須符合上一層 `_shared/`（本系列共用規範來源）的兩份規範：
  `../_shared/DEV_SPEC.md`（工程）、`../_shared/design/DESIGN_SPEC.md`（視覺）。
  **DEV_SPEC 第 0 節「鐵律」違反任一條即不合格。**
- 開發起手：複製 `../_shared/template/index.html`，**不從零寫**

### 教學意圖（決定了所有設計取捨）

國中生看得懂「車在跑」，看不懂「x-t 圖的斜率就是速度」。本模擬的全部價值在於
**三張圖與車的運動在同一條時間軸上對齊**，讓學生親眼看到：

- x-t 變陡的同時，v-t 的高度在上升
- v-t 曲線下的面積，就是 x-t 同一段時間的高度落差
- a-t 是一條水平線，且它決定 v-t 的斜率

**「三圖上下嚴格對齊、共用時間軸」是不可妥協的核心。**

---

## 2. 參考標的與四個改進點

台灣 CoSci 平台的 `Velocity and motion of uniform acceleration`（EJS 移植，
桃園市平興國中鍾沄蓁老師的探究學習單配套）：三個參數（初始位置、初速、加速度）、
一台車在 −200～200 m 軌道上跑、三個核取（每秒暫停／每秒軌跡點／速度箭頭）、
下方分頁切換 Datasheet｜X-t｜V-t｜a-t。

值得沿用：**參數只有三個**（剛好對應 x = x₀ + v₀t + ½at²）、**每秒軌跡點**把等加速度講得很直觀。

本模擬要贏過它的四點：

1. 它三張圖**分頁切換** → 學生看不到三圖對應關係。本模擬改為**堆疊＋共用時間軸＋游標貫穿**
2. 它跑完**不能回頭** → 本模擬加**時間軸 scrubber 拖曳回放**
3. 它**沒有斜率／面積工具** → 本模擬加 x-t 切線與 v-t 面積填色
4. 它**只有一台車** → 本模擬支援 1–4 台，可做等速 vs 等加速對照與追及相遇

---

## 3. 已定案的需求（使用者 2026-08-18 逐項確認，不要再問一次）

| 項目 | 決定 |
|---|---|
| 底部導覽分頁 | 兩個：**車子**（1～4 台）、**打點計時器**（本次只做「開發中」佔位頁，可點進去） |
| 車輛數 | 預設 1 台＝1 張摺疊卡；「＋ 新增車輛」增卡，**上限 4 台**；可刪除，但至少保留 1 台 |
| 三圖版面 | **垂直堆疊、共用時間軸**，一條時間游標線貫穿三張圖 |
| 教學輔助 | 每秒軌跡點、每秒自動暫停、x-t 切線斜率、v-t 面積填色（＝位移）。原本列的「車上速度向量箭頭」已於 **2026-08-20 裁示不做**，見下方修訂表 |
| 斜率／面積對象 | **只畫焦點車**（左欄選中的那台） |
| 邊界處理 | ~~座標軸自動延展~~ → **2026-08-21 改為三軸完全固定**（見第 3 節第三批），車跑出範圍就畫在圖外被裁掉，讀數仍正確；仍不撞牆、不中斷 |
| 時間軸回放 | **要**。拖曳 scrubber → 車與三圖游標同步 |
| 外觀流程 | **先由 Claude Design 出稿，再按稿實作**（全域規範 §10 協作鏈） |

### 參數範圍（已定，寫進 DESIGN_BRIEF）

| 參數 | 介面文字 | 檔位（只有這 5 格） | 預設 | 單位 |
|---|---|---|---|---|
| 初始位置 | 初始位置 x₀ | −100 / −50 / 0 / 50 / 100 | 0 | m |
| 初速度 | 初速度 v₀ | −20 / −10 / 0 / 10 / 20 ※第五批改 | 0 | m/s |
| 加速度 | 加速度 a | −4 / −2 / 0 / 2 / 4 | 0 | m/s² |

> **2026-08-21 使用者指定改為 5 檔制**，取代原本的「−100～100 步進 5／−40～40 步進 1／−10～10 步進 0.5」。
> 滑桿拖曳時一格一格跳（`step` ＝ 全幅的四分之一，瀏覽器原生吸附到最近格點）。
> 連帶影響：`a` 的上限由 ±10 縮到 ±4；`a` 的顯示小數位由 1 改為 0（`PARAM.a.dec`）；
> 預設車三個參數全為 0，**一開機車是靜止的**。

速度倍率：0.25×–2×（桌機滑桿、直向 0.5／1／2× 三格膠囊）。

### 2026-08-20 依設計稿定稿的修訂（使用者當場裁示，**取代上表與下列舊定案**）

設計稿的版面定稿 A／B／C 推翻了本節部分內容，稿內文字也有自相矛盾處。四項已裁示：

| 項目 | 定案 | 取代了什麼 |
|---|---|---|
| 時間軸總長 | **0–10 s** | 舊定案 0–20 s；稿 b4「對齊規則」卡與 b5「禁用（t = 20）」一併作廢 |
| 速度向量箭頭 | **不做** | 「教學輔助四項全做」中的箭頭項；稿 b8 難題 4 整節作廢（含它建議的 DESIGN_SPEC 改寫） |
| iPad 直向版型 | **按稿 C**：模擬區固定在上、車輛設定與顯示圖層並排在下，不做抽屜分頁 | DEV_SPEC 5.3／DESIGN_SPEC 第 1 節的「左右欄摺入抽屜成設定／數據分頁」——屬規範偏離，見第 4 節 |
| a-t 圖預設 | **摺疊成 40px 摘要條，可點開** | 「三張圖同時可見不可妥協」的絕對表述（摘要條仍顯示各車 a 值，數值資訊不損失） |

**其他照稿實作的結構性變更**（無需再裁示）：軌道改**垂直 4 欄制**（−200…200 m 由上往下，每台車獨占 54px 欄，4 欄合計 222px）；**軌道與 x-t 共用同一條縱軸**（軌道 0 m ＝ x-t 的 t 軸）；**刪掉右欄**改兩欄（桌機 `302px 1fr`、橫向 `262px 1fr`），焦點車讀數收進展開的車輛卡、顯示圖層移到左欄、每秒自動暫停移到抽屜；車名固定 **A～D** 且每卡一個眼睛鈕（顯示／隱藏）；左欄改**手風琴**（展開的那張＝焦點車）；顯示圖層只剩 x-t 切線、v-t 面積兩個核取。

### 2026-08-21 裁示（階段 2～6 開工前，取代 `HANDOFF.md` 的對應敘述）

| 項目 | 定案 | 取代了什麼 |
|---|---|---|
| 座標軸延展時機 | ~~改參數時一次算好整段範圍、250 ms 補間~~ **已於同日第三批廢止，改為三軸完全固定**（見下） | `HANDOFF.md` 第 5 節「車跑出範圍時延展、範圍只增不減」 |

理由：階段 3 要求曲線畫滿 0–10 s，軸若只跟到當前時刻，畫滿的曲線後半段會衝出圖框被裁掉；
且播放全程軸在動，學生的參照系會一直晃，違背「三圖對齊、看得懂」的教學核心。

### 2026-08-21 第三批：縱軸固定與教學輔助（使用者指定）

| 項目 | 定案 | 取代了什麼 |
|---|---|---|
| 三個縱軸 | **完全固定**：x ±200 m、v ±40 m/s、a ±4 m/s²，寫成常數 `POS_RANGE`／`V_RANGE`／`A_RANGE`，不隨參數或播放改變 | 整套自動延展機制（`niceStep`／`niceRange`／`computeRanges`／250 ms 補間／刻度淡出淡入）**已全部移除**，連同 `state.posRange`／`vMin`／`vMax`／`aRange` 四個欄位 |
| x-t : v-t 高度 | **2:1**（`track-block` `flex:2`、`vt-block` `flex:1`；a-t 展開時為 2:1:1）。實測 2.14–2.17:1，略高於 2 是因為各自的 18px 標題列不計入比例 | `track-block` 原本是**固定 300px**，v-t 卻是 `flex:1` 會吃掉所有多餘高度——視窗越高 v-t 越大、x-t 相對越窄，這正是「x-t 看起來太窄」的成因 |
| 連線後的點 | **保留**（`dotSeries()` 在 `done` 分支之後照樣執行） | 前一批的「連線後點不保留」 |
| 曲線的存續 | 新增 `state.drawn`：跑到過 10 s 就固定畫線，**往回拖時間軸線不再消失**。改參數、新增車、按「重新播放」會重設為 false（都是新的一輪） | 前一批用 `runFinished()` 直接看當下的 `state.t`（已移除） |
| 預設值 | `showTangent:false`、`autoPause:false`（HTML 的 `checked` 與 reset 還原值一併改） | 兩者原本預設開 |
| 參考線（新增） | x-t 圖上的當前點**水平虛線連回軌道上的車**，只畫焦點車，第三個核取方塊控制，**預設關**。車跑出 ±200 或焦點車被隱藏時不畫 | 無（新功能） |

軸範圍的選定理由：三者刻度正好對上滑桿的 5 個檔位（200/100/0/−100/−200、40/20/0/−20/−40、4/2/0/−2/−4），
且**等速 20 m/s 跑滿 10 s 剛好同時抵達 x 軸與 v 軸頂端**。
代價：v0 與 a 都拉到最大的極端組合（10 s 跑 700 m）會衝出圖外，讀數與膠囊數值仍正確。

### 2026-08-21 驗收後追加的四項（使用者指定）

| 項目 | 定案 | 取代了什麼 |
|---|---|---|
| 時間軸刻度 | 改為**每 1 秒一格**（scrubber 刻度點 1–9、三圖下方標籤 0–10、背景格線每秒一條）。**拖曳仍可停在任意時刻**，單步維持 +0.1 s | 原本每 2 秒一格 |
| 即時讀數位置 | 從車輛卡內移到**圖上跟著當前點走的膠囊**：x-t 標位置（`96.0 m`）、v-t 標速度（`28.0 m/s`），**不標時間**（時間在 header 已有）。膠囊會自動避開右邊界、頂端與 x-t 內側刻度 | 展開車輛卡內的「目前讀數」區塊（已整段移除，卡片只剩三個滑桿） |
| 曲線繪製時機 | **播放中三張圖都只描每秒一個點，t = 10 s 才連成線**；連線後點不保留。拖回 t < 10 會回到描點狀態 | 「曲線畫滿 0–10 s」（版面定稿 A／B／C 的畫法，以及 `HANDOFF.md` 第 5 節階段 3） |
| x-t 縱軸 | 在 **x-t 繪圖區左緣內側**再疊一組相同刻度（`z-index:3` 壓過 canvas，半透明白底），x-t 的軸名 `x` 因此移到右上 | 只有軌道左側那一組（讀 x-t 圖時視線要跨過 222px 的軌道） |

### 2026-08-21 審查後修訂（第五批，使用者逐項裁示）

四個 agent 對 v1 做了功能／認知負荷／觸控／文案審查，使用者依結果逐項裁示如下。

| 項目 | 定案 | 取代了什麼 |
|---|---|---|
| 初速度**滑桿**檔位 | **−20 / −10 / 0 / 10 / 20 m/s**。這是「學生能設的初速度」，**不是速度軸範圍**——v-t 軸一直是 −40～40 m/s，沒有動過；加速度仍可把速度推到 40 | 上表**初速度那一列**的 −40／−20／0／20／40 |
| 縱軸：繪圖範圍與刻度分家 | 繪圖 `POS_RANGE`／`V_RANGE`／`A_RANGE` = **220／44／4.4**，刻度 `POS_TICK`／`V_TICK`／`A_TICK` = **200／40／4**。刻度位置依繪圖範圍換算，故最大刻度落在 4.55%／95.45% 而非 0%／100% | 「三軸完全固定 ±200／±40／±4」——範圍與刻度本來是同一組數字，車停在 200 m 會貼在圖框邊緣 |
| 出界處理 | **任一台可見車的位置到 ±200 m 或速度到 ±40 m/s，該輪模擬就停在那一刻**。`firstHit()` 解二次式／一次式取最小正根，`simEnd()` 取所有可見車的最小值。時間軸擋在該刻不能再往前，讀數顯示 `t = 5.0 s ／ 5.0 s`，旁註「A 車已到 200 m」，scrubber 上出界後的區段畫斜線底。門檻＝最大刻度，因此畫面上永遠不會有東西被裁掉 | 「車跑出範圍就畫在圖外被裁掉，讀數仍正確」 |
| 單步 | **+1 s**（與軌跡點、格線、scrubber 刻度、每秒自動暫停同一個節奏） | 第四批的「單步維持 +0.1 s」 |
| v-t 面積 | **正負分塊**：v 過零處切開，各自在塊中央標該塊位移（`+50.0 m`／`−50.0 m`），塊太窄或太扁則不標；膠囊改標「位移合計」。**兩塊一律用車子的顏色**（顏色的職責是「這是哪一台車」），正負改靠**負塊加 45° 斜線網底**區分——2026-08-22 使用者裁示，取代同日稍早的「正塊藍 `#2563EB`／負塊紅 `#D63535`」分色版 | 單一車色的自交四邊形靠 nonzero 填法分塊——兩塊同色又無紋理，視覺上讀不出正負，膠囊卻給帶號淨值 |
| UI 等比放大 | 新增 `--u` 係數與 `uiScale()`：以實體像素（`screen.width × DPR`）判裝置級別，大屏再依 CSS 寬度換算，使字的**物理高度**不隨系統縮放設定改變（3840 大屏在 Win 100%／150%／200% 下都是 8.9 mm、6 公尺外 5.1 弧分）。CSS 53 處字級與關鍵尺寸改 `calc(Npx * var(--u))`，canvas 內字級／線寬／點徑／軌道欄位座標乘 `U` | 無（新機制）。原本 ≥1101px 沒有任何斷點，且 canvas 內 9／10／12px 是寫死的字面量，media query 管不到 |
| 觸控目標 | 眼睛／垃圾桶／展開箭頭 36→**44** 寬，自動暫停開關命中高 24→**44**，a-t 摺疊條與彈窗關閉鈕 40→**44**；刪除與箭頭間距 2→4px（中心距 38→48） | 第 4 節偏離 3「三顆卡內鈕維持 36×44」 |
| 車輛列表捲動 | `.cars-list input[type="range"]` 改 `touch-action:pan-y`，時間軸 scrubber 維持 `none` | 全域 `input[type="range"]{touch-action:none}` 一體適用，導致手指落在滑桿上就捲不動列表、還會誤改參數 |
| 卡片壓縮 | `.car-name` 由 `flex:none` 改 `flex:1 1 auto`，摘要加 `text-overflow:ellipsis` | 摘要文字不能收縮 → 窄欄時把右側按鈕擠出卡片，被 `overflow:hidden` 裁掉 |
| 按下回饋 | 所有可點元素加 `:active{opacity:.55}`，圓鈕與新增鈕另加主色底 | 無——`-webkit-tap-highlight-color:transparent` 關掉系統回饋後沒有補上自己的 |
| 曲線辨識 | 非焦點車的 x-t／v-t 曲線在右端標車名字母 | 只靠顏色與線寬 |
| 對比度 | `--c-text-3` #7A8496→**#67717F**（3.77→4.95:1）、`--c-ok` #2F9E63→**#24794C**（3.39→5.36:1）、C 車曲線 #E8871E→**#D9761A**（2.65→3.20:1）、隱藏車摘要 #9AA3B2→`--c-text-3`（2.37→4.61:1）、格線與軸線加深 | 原配色：多數次要文字只有 3.77:1，低於 AA 的 4.5 |
| 文案 | x-t 區塊標題由開發者的版面計算註記改為「位置 x (m)」；`.col-label` 寬度 50→54px（與 `COL_W` 對齊，兩者是同一次漏改的殘跡）；「速度」→「播放速度」（與 v-t 的「速度 v」撞名）；刪「拖曳可停在任一時刻」與「每秒軌跡點與秒數一律顯示」；a-t 摺疊條副標只留「已摺疊」；說明彈窗刪掉 ½at² 公式並統一人稱 | v1 文案 |

#### 放大機制的三個陷阱（2026-08-22 使用者目視發現「四欄不等距」後追查出來，接手前務必讀）

`--u`（CSS）與 `U`（JS）是同一個係數的兩份副本，canvas 用 `U` 排版、版面用 `--u`，**兩者一旦不同步，
軌道的四欄就會與 canvas 畫的欄位錯開**——這就是使用者看到的現象。三個各自獨立的成因：

1. **初值沒同步給 CSS**（根因）。`applyScale()` 只在「值有變化」時才寫 CSS 變數，而 `let U = uiScale()`
   在載入當下若已算出最終值，`applyScale()` 就 `return false`，`--u` 永遠停在 `:root` 的預設 1。
   於是 canvas 按 U=1.15 排四欄（總寬 255px），軌道 CSS 寬卻只有 222px，第四欄被切掉。
   → 宣告 `U` 之後**立刻**寫一次 CSS 變數，不要依賴 `applyScale()`。
2. **canvas 端有座標漏乘 `U`**。軌道分隔線與參考線起點原本直接用 `TRACK_PAD + COL_W * i`。
   → 一律改走 `colX(i)`，不要在別處重算欄位座標。
3. **欄標是 JS 算進 inline style 的**，`--u` 改變後不會自己更新（`fitAll()` 不呼叫 `renderTrackLabels()`）。
   → 改用 `left:calc(Npx * var(--u))`，交給 CSS 自動跟隨。

4. **`--plot-left` 是一個加總，它的每一項都要放大**。它的值是 `48（縱軸）+ 10（間距）+ 222（軌道）+ 10`，
   而 v-t／a-t 直接拿它當左側留白、x-t 卻是由那四段實際堆出來的。先前只有軌道乘了 `--u`，
   於是 U=1.9 時 x-t 的繪圖區左緣比 v-t 少 61px，**三張圖的時間軸整個錯開**。
   → `.yaxis` 寬、`.track-row` 的 gap、`.plot` 左框線、`.taxis` 的 padding 補正，全部要乘 `var(--u)`。
   驗收：三張圖的 `left` 與 `w` 必須一致到 0.5px 以內。

另外 `screen.width` 在部分環境載入後仍會回報 0 一段時間，`uiScale()` 會誤判成桌機；而 ResizeObserver
只在版面尺寸變化時觸發，係數沒變版面就不變，形成死結永遠卡在 1。已加 `load`／`resize` 監聽與一段
輪詢（最多約 5 秒）補算。**但自動判定終究不可靠**，因此新增網址參數：

| 手段 | 用法 |
|---|---|
| 強制指定放大倍率 | `index.html?u=1.9`（範圍 1–4，蓋過自動判定）。教室大屏把帶參數的網址存成書籤即可 |
| 放大倍率的天花板 | `clampScale()`：`min(u, 4, 視窗寬/900, 視窗高/620)`。**手動指定也吃這個上限**——1280×720 上要 1.9 只會得到 1.16。不 clamp 的話，時間軸列的 padding 會超過容器寬被 flex 擠成負值，三張圖也會被放大的標題列吃到只剩幾十 px 高 |

驗收方式：四台車全開，量 `分隔線／車／軌跡點／秒數／欄標` 五組的相鄰間距，五組都必須等於 `54 × U`。
實測 U = 1／1.15／1.9／2.3 四檔皆通過。

#### 2026-08-22 版面位置調整（使用者指定）

| 項目 | 定案 | 取代了什麼 |
|---|---|---|
| 三張圖的區塊標題 | 一律對齊繪圖區左緣（`.track-head／.vt-head／.at-head` 共用 `padding-left:var(--plot-left)`） | x-t 的標題原本貼在整個畫面最左邊，與另兩張不齊 |
| v-t／a-t 的縱軸刻度 | 移到**繪圖區內側**（`.yaxis-in`，半透明白底），與 x-t 一致；軸旁那組移除——它們左邊沒有軌道隔開，兩組數字會貼在一起重複 | 刻度畫在 `.yaxis-v`（繪圖區左外側） |
| 三張圖的軸名 | `x`／`v`／`a` 一律移到繪圖區右上 | v-t／a-t 原本在左上，會與新的內側刻度重疊 |

**使用者明確否決的審查建議**（不要再提）：軌跡點在固定軸下前段擠在一起（不修正）；改參數後即時看形狀變化、不必等跑完 10 秒（不做）。
**稿件已知的文字損毀處**（Design 工具字串錯位，實作時按合理值解讀）：稿第 1186 行「BD車.0」應為「B車 4.0」；第 1211 行「焦點C車.5px、其他B車px」應為「焦點 3.5px、其他 2px」；第 1723 行提到的「右欄數據卡」在定稿中已不存在。

---

## 4. 技術決定（接手者請遵循，變更前先問使用者）

- **不引入 Chart.js 或任何圖表函式庫**。DEV_SPEC 第 1 節要求引入函式庫前先問使用者；
  且三張圖要逐幀與動畫同步、還要疊切線與面積填色，函式庫反而綁手綁腳並拖累 iPad 效能。
  **三張圖以 canvas 自繪。**
- **物理用公式解，不做數值積分**：

  ```
  v(t) = v₀ + a·t
  x(t) = x₀ + v₀·t + ½·a·t²
  ```

  好處：拖曳回放能直接算出任意 t 的完整狀態；重置乾淨；圖表資料不必逐幀累積成
  無限成長的陣列（DEV_SPEC 6.4 明文禁止）。

- **canvas 分四層**（DEV_SPEC 6.4 要求靜態／動態分層）：
  - 軌道背景（欄分隔線、0 m 基準線）／軌道前景（車體、每秒軌跡點、秒數標號）
  - 圖表背景（三組座標軸、格線、軸標）／圖表前景（曲線、時間游標、切線、面積填色）
  - **三張圖畫在同一個 canvas 的三個繪圖區**，共用同一組時間軸座標映射函式，
    才能保證上下嚴格對齊（分成三個 canvas 容易因 DPR 取整而差 1–2px）
  - 圖表背景只在版面尺寸變更時重繪（縱軸已固定，不再有範圍變動這個觸發源）
- `update(dt)` **純計算不碰 DOM**、`render()` **只讀 state**（DEV_SPEC 第 1 節硬要求）
- state 集中在單一物件；**重置必須完整還原**（含軌跡點與圖表資料）
- 互動一律 **Pointer Events ＋ setPointerCapture**（DEV_SPEC 鐵律 5），scrubber 與滑桿都是
- DPR 鉗上限 2、`dt` 鉗上限 50ms、分頁隱藏時暫停（DEV_SPEC 6.1 / 6.2 骨架直接抄）

### 規範偏離（已裁示，實作時照本節）

| # | 偏離內容 | 理由 | 要解除需要什麼 |
|---|---|---|---|
| 1 | **速度箭頭配色衝突已消滅**：本模擬不做速度箭頭 | 垂直軌道 4 欄制下每台車只有 54px 欄寬，箭頭要沿垂直方向畫，會與同欄的每秒軌跡點、秒數標號重疊；而「軌跡點間距逐次拉開」本身就是速度的視覺表徵 | 使用者要求加回箭頭（屆時要重新處理「箭頭橘色 vs 車別序列色」的衝突） |
| 2 | **iPad 直向不摺入抽屜分頁**，改為模擬區在上、設定在下（稿 C） | 本模擬已無右欄數據，只剩「設定」一組內容，摺成抽屜分頁反而多一次點擊 | 使用者要求回歸 DEV_SPEC 5.3 的系列一致寫法 |
| 3 | **車輛卡內的刪除／摺疊鈕容器撐成 36×44**（稿畫的是 30×30） | 稿的 30px 不滿足 DEV_SPEC 鐵律 6「觸控目標 ≥ 44×44」；視覺圖示維持 13px 不變，只放大命中區 | 無（這是往規範靠攏，不是偏離規範） |
| 4 | **展開的車輛卡內不顯示滑桿 min/max 列**（稿 E 元件狀態稿有畫） | 版面定稿 A 的展開卡就沒有這一列；加上後展開卡 334px > 左欄可用 311px，只有 1 台車就開始捲動、第三個滑桿被裁掉 | 左欄高度預算變寬鬆時可加回 |
| 5 | **直向抽屜排成兩列**（scrubber 獨佔整寬），未照稿 C 的單列 | 稿 C 的抽屜是桌機版複製未改；直向照抄會讓 scrubber 只剩約 186px，違反「scrubber 要夠大夠好拖」的核心需求。兩列排法與稿 E 的直向速度膠囊、稿 b8 難題 3 一致 | 無 |

---

## 5. 開發階段（每階段結束停下，附驗證步驟等使用者確認）

### 階段 0 — 需求定案與 design brief ✅ 已完成（2026-08-18）
產出 `DESIGN_BRIEF.md`、本檔。等 Claude Design 出稿。

### 階段 1 — 骨架與版面（按設計稿）✅ 已完成（2026-08-20）
從 `../_shared/template/index.html` 複製起手 → HTML 結構、CSS tokens、單一 state 物件、
車輛卡手風琴（新增／刪除／摺疊／眼睛／選焦點車）、顯示圖層、抽屜控制列、兩個底部分頁、
打點計時器佔位頁、三種尺寸響應式；模擬區兩層 canvas 就位，背景層畫出軌道欄線、0 m 基準線、
三圖格線與零基線，前景層畫時間游標。

**比計畫多做的一項**（刻意，交付時已說明）：時間推進真的接上了——播放／暫停／單步／重置／
速度倍率／scrubber／每秒自動暫停全部可操作，並連動時間游標與車輛卡讀數（讀數走公式解）。
理由是若不接，抽屜整排控制項會是死的，違反「不留按了沒反應的控制項」，階段 1 也無從驗證。
**仍未做**（階段 2／3 的範圍）：車體、每秒軌跡點與秒數、三張圖的曲線、切線與面積填色。

### 階段 2 — 物理模型與軌道動畫 ✅ 已完成（2026-08-21）
軌道車體繪製（canvas 幾何圖形，非 FA 字符）、位置刻度尺、每秒軌跡點與秒數、
多車同軌道（4 欄制）。**不做速度箭頭**（見第 4 節偏離 1）。
（原列的「座標軸自動延展」已於 2026-08-21 第三批廢止，改為三軸固定。）
**驗證（手算對照，必須逐項核對數值而非只看動畫像不像）**：
下表三組已在階段 1 用讀數卡驗過（公式解正確，含 −0.0 與 U+2212 負號），
階段 2 要驗的是**繪製結果**：軌跡點落點、間距比例、車體位置與 x-t 曲線同高。

| 輸入 | t = 3 s 時的預期值 |
|---|---|
| x₀=0, v₀=20, a=0 | x = 60 m、v = 20 m/s（等速：軌跡點等距） |
| x₀=0, v₀=0, a=2 | x = 9 m、v = 6 m/s（軌跡點間距 1:3:5） |
| x₀=50, v₀=−20, a=4 | x = 8 m、v = −8 m/s（先往負向，t=5 s 時 x=0、v=0 再反向） |

> 第三組原為 `x₀=50, v₀=−10, a=2`，但 2026-08-21 改 5 檔制後 **v₀=−10 已不在檔位上**（滑桿設不出來），
> 改用上表的 `v₀=−20, a=4`——折返點同樣落在 t = 5 s。已於 2026-08-21 實測通過。

### 階段 3 — 三張堆疊圖 ✅ 已完成（2026-08-21）
共用時間軸的座標映射、三組軸與格線（背景層）、多車曲線、時間游標同步。
**驗證**：等速車的 x-t 是直線、v-t 是水平線、a-t 貼在 0；等加速車的 x-t 是拋物線；
三圖左右邊緣像素對齊（拉一條垂直線比對）；4 台車時顏色與軌道車體一致。

### 階段 4 — 切線斜率與 v-t 面積（焦點車）✅ 已完成（2026-08-21）
x-t 游標處切線 ＋ 斜率數值標註；v-t 的 0→t 面積填色 ＋ 位移數值。
**驗證**：切線斜率數值 = 同時刻 v-t 圖高度 = 右欄 v 數據卡（三者必須一致）；
v-t 面積數值 = x(t) − x₀（用上表手算組核對）。

### 階段 5 — 時間軸拖曳回放 ✅ 已完成（2026-08-21，繪製端不需修改，驗證通過）
scrubber 拖曳 → 由公式反算該時刻所有車的狀態 → 軌道與三圖同步。
**驗證**：播放中拖曳、暫停中拖曳、拖到 0、拖到 10 s 末端、快速來回拖；
拖曳時軌跡點要正確地增減；放開後接續播放的行為要正確。

### 階段 6 — 收尾 ✅ 已完成（2026-08-21，三項留給真機）
打點計時器佔位頁、`<900px` 響應式摺疊、DEV_SPEC 第 8 節「交付前檢核清單」逐項回報 ✅/❌。
**驗證**：DevTools 4× CPU throttling 跑 10 秒 ≥30fps；`file://` 雙擊直開可運作；
全文搜尋 Unicode 上下標字元零命中；切分頁 3 秒回來不跳格。

#### 階段 2～6 的實測數值（2026-08-21，Claude Browser pane，canvas API spy ＋ 像素掃描）

| 驗證項 | 結果 |
|---|---|
| 手算三組（t = 3 s） | x/v ＝ 60/20、9/6、8/−8，三組皆與讀數卡一致（負號為 U+2212）。第三組於 2026-08-21 改 5 檔制後重驗 |
| 軌跡點間距比 | 等速 **1 : 1 : 1**；等加速 **1 : 3 : 5**；減速折返 **1 : 0.778 : 0.556**（皆為繪製座標實測，非重算公式） |
| 車體與 x-t 曲線同高 | 兩者 y 座標相同至小數點後三位（161.207 ＝ 161.207） |
| 三者一致 | 切線膠囊 50.0 ＝ 車輛卡 v 50.0 ＝ v-t 曲線像素反推 49.998 |
| 面積 ＝ 位移 | 膠囊 125.0 ＝ x(5) − x₀ ＝ 125；填色 alpha 56/255 ≈ 22%，t 之後無填色 |
| 切線幾何 | 端點精確落在 ±4 s，6px 白底 ＋ 2px 虛線 `[7,5]` |
| 軸固定 | 把三個滑桿全部拉到最大後，三軸刻度完全不變；`yPos()` 把 200／0／−200 精確映射到圖頂／正中／圖底 |
| 參考線 | 起點精確落在車體中心（83.667）、終點落在 x-t 當前點（585.733），y 同時等於軌道車位置與 x-t 曲線高度（97.591）——完美水平 |
| 曲線存續 | t=3 描點無線 → t=10 連線且點保留 → 拖回 t=5／t=0 線仍在；改參數與重新播放則收回成描點 |
| 拖曳回放 | t = 0/2.5/7/10/3/0 → 軌跡點 1/3/8/11/4/1 顆；pointerdown 即自動暫停 |
| 三圖左右對齊 | 1366×768、1024×768、768×1024 三種尺寸、a-t 展開與摺疊皆嚴格相等 |
| 每幀成本 | 最重情況（4 台車 ＋ a-t 展開 ＋ 曲線 ＋ 33 顆描點 ＋ 切線 ＋ 面積 ＋ 參考線）`render()` 中位數 **0.925 ms**。⚠ **這是 JS 端送出 canvas 指令的時間，不含 GPU 光柵化**——頁面在驗證環境是 hidden 的，不合成畫面，所以這個數字不能拿來推論 fps，真實流暢度仍須真機實測 |

### 部署後（使用者在真機執行，本機驗不準）
iPad Safari 開部署網址：版面、字體、動畫流暢度、觸控拖曳；Windows Chrome/Edge 同上。

---

## 6. 給下個 session 的接續開場 prompt

> **⚠ 2026-08-20 起：下一個 session 請改讀同目錄的 `HANDOFF.md`**——那是「階段 2～6 一次做完」的
> 完整工作單（含各階段實作規格、稿件關鍵數值、驗證方法與環境限制），使用者已指定範圍與節奏。
> 本節以下的開場 prompt 保留作為**通用模板**，`HANDOFF.md` 在階段 6 完成後失效。

> 以下整段可直接貼進新 session。

```text
接續開發 motion-graphs-sim（國中理化「直線運動」模擬器，呈現 x-t / v-t / a-t 三圖）。

專案目錄：C:\Users\Hope\Desktop\CLAUDE工作區\simulation\motion-graphs-sim\
（`simulation/` 底下：`_shared/` 是共用規範，其餘每個資料夾都是一個獨立模擬 repo）

動手前先完整讀過這幾個檔案（路徑相對於專案目錄）：
1. PROJECT_PLAN.md —— 進度、已定案需求、技術決定、階段計畫。這是權威進度來源。
   **第 3 節的「2026-08-20 修訂表」取代了它上方的舊定案，兩者衝突時以修訂表為準。**
2. index.html —— 現行程式碼（階段 1 的骨架與版面，單一檔案內嵌 CSS/JS）。
3. design_handoff/直線運動模擬器 設計稿.dc.html —— 外觀依據。
   **只有版面定稿 A／B／C（`#b1`/`#b2`/`#b3`）與元件稿 E、佔位頁 F、標註 G 有效；
   難題節 H（`#b8`）與 D 稿的部分說明卡是被定稿推翻的舊方案，不要照做。**
4. ../_shared/DEV_SPEC.md —— 工程規範。第 0 節「鐵律」違反任一條即不合格。
5. ../_shared/design/DESIGN_SPEC.md —— 視覺規範。

規則：
- PROJECT_PLAN.md 第 3 節的已定案需求（含修訂表）是使用者逐項確認過的，不要重新徵詢。
- 先列出本階段的實作子步驟清單，經我確認後再動手。
- 每個階段結束停下，附「怎麼驗證」的具體步驟，不要自己往下一階段衝。
- 物理正確性用 PROJECT_PLAN 第 5 節階段 2 的手算對照表核對，不是只看動畫像不像。
- 版面驗證用 getBoundingClientRect 斷言，不要只看截圖——尤其「三圖左右邊緣嚴格對齊」
  這條核心需求，要斷言 x-t／v-t／a-t 三者的 left 與 right 完全相等。
- 完成後執行 DEV_SPEC.md 第 8 節「交付前檢核清單」，逐項回報 ✅/❌。

本次要做的是：階段 2（物理模型與垂直軌道動畫）
設計稿狀態：已出稿，在 design_handoff/
```

---

## 7. 本專案刻意沒做（限制與解除條件）

| 項目 | 理由 | 要解除需要什麼 |
|---|---|---|
| ~~打點計時器只做佔位頁~~ **已於 2026-08-24 解除** | 使用者已提出完整需求細節（點距、頻率、紙帶呈現方式），規格與階段計畫見**第 8 節**。本列保留作為歷史紀錄 | — |
| 不做自由落體／斜面 | 本模擬聚焦一維等加速度，避免一個模擬塞兩個概念 | 另開新模擬，不擴充本專案 |
| 不做學習單／挑戰任務模式 | 參考網頁有 Worksheet／Cooperation Mode，本次不跟進 | 使用者提出題庫與判定規則 |
| ~~未建 GitHub repo、未部署~~ **已完成（2026-08-25）** | 原本使用者指定階段 2～6 停在本機 commit | — 已建 `hfuhsu108/motion-graphs-simulator`（公開）並開啟 Pages |
| 未驗到「真的按下播放跑動畫」 | Claude Browser pane 的頁面處於 hidden，rAF 不執行（pitfalls-services 第 3 條）；已改用手動迴圈呼叫 `update(dt)` ＋ `render()` 推進 700 幀到 t = 10，驗證推進邏輯、每秒自動暫停與繪製端無例外 | 使用者在真實瀏覽器按一次播放確認 |
| 未驗到 `file://` 雙擊直開 | Browser pane 把專案外的 file:// 檔案渲染成靜態快照，量不到實際行為；已改用靜態證據（無 `fetch`／`XMLHttpRequest`、無 `/` 開頭絕對路徑、唯一外部資源是 FA CDN） | 使用者雙擊 `index.html` 確認 |
| 未量到 4× CPU throttling 的實際 fps | Browser pane 不合成畫面，量不到真 fps。改量 `render()` 的 JS 端成本（中位數 0.56 ms／幀），但**該數字不含 GPU 光柵化，不足以推論 fps** | 使用者用 DevTools Performance 實測 |
| 未取畫面截圖 | Browser pane 未顯示時截圖一律 5 秒逾時（HANDOFF 第 6 節已載明），全部改走 canvas API spy ＋ 像素掃描的機器斷言 | 使用者自己開一次瀏覽器看 |
| 位置軸維持**對稱** ±posRange | 為了讓軌道的 0 m 基準線恆在正中央。代價：只往單一方向跑的車只用得到一半高度，運動前段的軌跡點被壓縮（把 a 推到 2 之後，t = 3 s 時四顆點落在約 8px 內，看不出 1:3:5，秒數也只標得下「0」） | 使用者同意改成非對稱（軸取實際值域但必含 0），高度利用率約翻倍 |
| 參考線只畫焦點車 | 與切線、面積一致；四台車全開時四條水平虛線交錯，反而看不出哪條對應哪台 | 使用者要求全部畫（`drawGuide()` 改成遍歷 `state.cars`） |
| 切線與面積在描點階段照常顯示 | 兩者由公式決定，不依賴曲線畫出與否；面積填色本身就描出了 v 的變化 | 使用者覺得沒有曲線時看起來突兀，要求延到連線後才出現 |
| 本目錄尚無自己的 `.claude/launch.json` | 母倉 `simulation/.claude/launch.json` 已有 `motion` 條目（port 4175，serve root 限本目錄），在母倉開 session 時夠用 | 等本目錄 `git init` 成獨立 repo 時，複製一份 launch.json 進來（serve root 寫 `.`），讓 clone 下來就能跑 |

---

## 8. 打點計時器分頁（2026-08-24 討論定案，尚未開工）

> 本節是「打點計時器」分頁（`#screen1`）的完整規格，取代第 7 節「打點計時器只做佔位頁」那一列。
> 使用者以三輪 AskUserQuestion 逐題裁示，**下個 session 不要重新徵詢本節已定的事**。
> 流程上**不出 Claude Design 設計稿**（使用者 2026-08-24 指定）：版面骨架完全沿用「車子」分頁，
> 真正新增的只有紙帶區的繪製細節，再跑一輪設計稿的邊際效益不足。

### 8.1 定位

「車子」分頁教的是**圖與運動的對應**；打點計時器分頁教的是**真實實驗怎麼把運動變成數據**——
量點距、除以時距、得到平均速度，再由平均速度的變化得到加速度。兩頁的關係是
「同一個概念、兩種測量方式」，**不是同一組參數**（量級差兩個數量級：車子頁是 ±200 m／±40 m/s，
紙帶是 0–40 cm／0–8 cm/s，把車子頁的 v₀ = 10 m/s 拿來拉紙帶，0.04 秒就跑完全程）。

佔位頁現有文案承諾的「與車子分頁的同一組參數對照」與「每 1/60 秒打一個點」**兩項都作廢**，
理由分別是上述量級問題、與 60 Hz 在 40 cm 內畫 600 個點看不見（見 8.3）。

### 8.2 已定案需求（使用者 2026-08-24 逐題確認）

| 項目 | 決定 |
|---|---|
| 位置 | `index.html` 的 `#screen1`，**取代**現在的「開發中」預告頁 |
| 版面骨架 | 沿用「車子」分頁：Header 50 ／ 左欄設定卡＋舞台卡 ／ 底部抽屜 ／ 底部導覽 56 |
| 三張圖 | **完全不動**：x-t : v-t = 2:1、a-t 摺疊條、共用時間軸、內側縱軸、右上軸名、圖上跟著走的讀數膠囊。使用者原話：「x-t 圖、v-t 圖、a-t 圖的版面配置都不要動」 |
| 紙帶方向 | **垂直**，與 x-t 共用位置縱軸。計時器固定在 0 cm（畫面底），紙帶往上被抽走 |
| 紙帶模型 | **真實版：紙帶跟著車走，點跟著紙帶漂移**（使用者否決了「固定紙帶、點留原地」的簡化版，見 8.5） |
| 驅動方式 | 情境選擇（五個）＋ 手動拉紙，兩者以「操作模式」單選切換 |
| 手動拉紙 | 按 ▶ 開始計時 → 拖台車 → t = 10 s 或到 40 cm 自動停 → 之後可拖時間軸回放 |
| 打點頻率 | **0.5 / 1 / 2 Hz** 三檔膠囊，預設 1 Hz。文案要誠實註明「真實電磁計時器每秒打 60 點，此處放慢以便觀察」 |
| 描點節奏 | **與打點同步**：紙帶打一點，三張圖同時描一點。頻率改 2 Hz，圖上的點就變兩倍密 |
| 連線時機 | 跑到 t = 10 s（或提前停在 40 cm）才把描的點連成線，與車子分頁一致 |
| 顯示圖層 | **四項全做**：公分量尺、x-t 切線、v-t 面積、參考線 |
| 測量互動 | **點選兩點測距**：點紙帶上任兩點 → 標 Δx、Δt、平均速度，**同時在 x-t 圖上高亮對應的兩點並標出同一個 Δx** |
| 抽屜 | 播放‧單步‧重置 ｜ 播放速度 ｜ **打點頻率膠囊** ｜ **每次打點自動暫停**（取代「每秒自動暫停」）｜ 時間軸 scrubber |
| 單步 | 前進**一個打點間隔**（1 Hz = 1 s、2 Hz = 0.5 s、0.5 Hz = 2 s） |
| 到底怎麼停 | 車到 40 cm 就停在那一刻，沿用車子分頁 `firstHit()` / `simEnd()` 的機制與 scrubber 斜線底標示 |
| 讀數膠囊 | x-t 標位置（`25.0 cm`）、v-t 標速度（`2.5 cm/s`），沿用現有寫法 |
| 說明彈窗 | 打點頁另寫一份「這個模擬要教什麼」，不與車子頁共用 |
| 導覽列 | 做完後移除「開發中」佔位頁，還掉 `DESIGN_SPEC` 第 1 節「未完成的功能不得出現在導覽列」那筆債 |
| 車輛數 | 只有一台，左欄**不要**手風琴、不要新增／刪除／眼睛鈕 |

### 8.3 物理與尺度

| 情境 | 介面文字 | v₀ (cm/s) | a (cm/s²) | t = 10 s 的位置 | 紙帶上看到什麼 |
|---|---|---|---|---|---|
| 1 | 靜止 | 0 | 0 | 0 cm | 點全疊在計時器那一格 |
| 2 | 慢穩 | 2.5 | 0 | 25 cm | 點距相等、較小 |
| 3 | 快穩 | 4 | 0 | 40 cm | 點距相等、較大 |
| 4 | 慢而快 | 0 | +0.8 | 40 cm | 點距 1:3:5:7 逐次拉開 |
| 5 | 快而慢 | 8 | −0.8 | 40 cm（t = 10 剛好停住） | 點距逐次縮小到零 |

情境 4 與 5 刻意設計成對稱（同樣 40 cm、同樣 |a| = 0.8），可以並排講「加速 vs 減速」。

**軸範圍**（比照車子分頁「繪圖範圍與刻度分家」的做法，繪圖範圍留貼邊餘裕）：

| 軸 | 刻度上限 | 繪圖範圍 | 說明 |
|---|---|---|---|
| 位置 | `POS_TICK_T = 40` cm | `POS_RANGE_T = 44` | **單向 0–40，非對稱**（0 在底、40 在頂）。紙帶只能單向抽出，對稱軸會浪費一半高度 |
| 速度 | `V_TICK_T = 8` cm/s | `V_RANGE_T = 8.8` | 單向 0–8（手動模式車不能倒退，v ≥ 0） |
| 加速度 | `A_TICK_T = 1` cm/s² | `A_RANGE_T = 1.1` | **對稱 ±1**（情境 5 的 a 是負的） |

位置換算式與車子分頁的 `yPos()` 不同（那是對稱軸），打點頁要另寫：

```
yPosT = (r, x) => r.y + (POS_RANGE_T - x) / POS_RANGE_T * r.h    // x ∈ [0, 40]
```

1 Hz 時 10 秒打 10 個點、平均點距 4 cm，量得到；2 Hz 是 2 cm、0.5 Hz 是 8 cm，三檔都可讀。
**60 Hz 不做**：10 秒 600 個點擠在 40 cm 內，點距 0.06 cm，看不見也量不到。

### 8.4 版面規格

```
│←48→│←──────────── 222px ────────────→│
│縱軸 │  46  │    紙帶 130    │   46    │        x-t 繪圖區
│40cm┤│ 量尺 │ ●0 ← 0 秒的點   │  🚗 ←──┼───────── ● 當前點（參考線連這兩個）
│30  ┤│ (綠) │ ●1  1 秒        │         │      ╱
│20  ┤│ 可拖 │ ●2  2 秒        │         │   ╱
│10  ┤│      │ ●3  3 秒        │         │ ╱
│ 0  ┤│      │▉●4 ← 最新的點   │         │╱
      └ 計時器固定在 0 cm
```

- **222px 沿用車子分頁的軌道區寬度**（`--plot-left` 的組成不變，`--u` 放大規則照舊，見第 3 節「放大機制的三個陷阱」）
- 三欄分法：量尺 46 ｜ 紙帶 130 ｜ 車軌 46
- **量尺只在勾選時出現，佔的是紙帶左側原本就留白的 46px**，因此版面不跳、也不遮住點
- 量尺可上下拖曳（Pointer Events ＋ setPointerCapture），**0 點吸附到最近的打點**——真實量法就是把尺的 0 對準第一個點
- 秒數標號每個點都標；2 Hz 時改為每 2 點標一次，避免標號打架
- 左欄第一張卡改成「情境設定」：操作模式（情境選擇／手動拉紙）＋ 五個情境單選

### 8.5 紙帶模型（最容易寫錯的地方，實作前務必讀懂）

計時器固定在畫面 0 cm，紙帶上端接在車上，**紙帶被抽出的長度恆等於車走過的距離 x(t)**。
`tₙ` 時刻打的點，是打在「當下的打點頭位置」，也就是畫面 0 cm；之後紙帶隨車往上跑，於是：

```
第 n 個點在時刻 t 的畫面高度 = x(t) − x(tₙ)
```

由此推出四件必須寫對的事：

1. **t = 0 的點永遠貼在車底下**（高度 = x(t) − 0 = x(t)），最新打的點永遠在 0 cm。
   所有點都落在 [0, x(t)] 之間，**永遠不會溢出畫面**，不需要裁切邏輯。
2. **點的絕對高度不等於 x-t 圖上點的高度**，兩者差一個上下鏡像。
   因此**不要**試圖讓紙帶上的點與 x-t 圖上的點水平對齊——那是做不到的。
3. **相鄰點距 = `x(tₙ₊₁) − x(tₙ)`，不隨 t 改變**。量尺量到的值永遠正確，這是本模擬的教學核心。
4. **共用縱軸仍然成立且有意義**：車本身在高度 x(t)，與 x-t 圖的當前點同高，
   **參考線（水平虛線）連的就是「軌道上的車」與「x-t 上的當前點」**，與車子分頁的參考線語意完全一致。
   紙帶點與 x-t 的對應則改由「點選兩點測距時在兩邊同時標出同一個 Δx」來建立。

> 使用者在 2026-08-24 討論中**明確否決**了「固定紙帶、點留在原地」的簡化版
> （那個版本能讓紙帶點與 x-t 點嚴格水平對齊，但畫面上看不到紙帶被拉走的動態）。
> 不要再提，也不要偷偷改回去。

### 8.6 三張圖的資料怎麼來（本節是本分頁與車子分頁最大的內在差異）

**三張圖全部由紙帶點距推出來**，而不是由公式直接求值——因為這正是打點計時器的教學鏈：

```
點距 ÷ 時距 = 該段平均速度  →  平均速度的變化 ÷ 時距 = 加速度
```

| 圖 | 資料點 | 標在哪個時刻 |
|---|---|---|
| x-t | `x(tₙ)`，每個打點時刻一個 | `tₙ` |
| v-t | `(x(tₙ) − x(tₙ₋₁)) / Δt`＝第 n 段的**平均速度** | **該時距的中點** `(tₙ₋₁ + tₙ)/2` |
| a-t | 相鄰兩個平均速度的差 ÷ Δt | 那兩個 v 點的中點 |

**為什麼 v 要標在時距中點**：等加速度運動的區間平均速度，恰等於區間中點的瞬時速度。
標在中點，v-t 的點就會漂亮地落在真正的 v-t 直線上；標在區間末端會整體偏移半格，
學生連線後會發現斜率對、截距不對。這一條同時解決了手動拉紙模式（沒有公式可用，只能差分）
與情境模式（有公式，但要與手動模式的圖表語意一致）。

連帶後果，實作時要一併處理：
- v-t 的第一個點要到**第 2 次打點**才出現，a-t 的第一個點要到**第 3 次打點**才出現
- x-t 切線（瞬時速度）與 v-t 的點（平均速度）在等加速度下會有半格的視覺差異，
  這是**真實的物理差異，不是 bug**，說明彈窗要講一句

### 8.7 開發階段（每階段結束停下，附驗證步驟等使用者確認）

| 階段 | 內容 | 驗證方式 |
|---|---|---|
| **T1** 分頁骨架 | 左欄改「情境設定」卡；抽屜內容依 `data-screen` 切換（打點頁顯示打點頻率膠囊與「每次打點自動暫停」）；移除 `.app[data-screen="1"] .drawer{display:none}` | 兩頁來回切換，斷言各自的左欄與抽屜內容正確、切回車子頁時原狀態未被破壞 |
| **T2** 紙帶區與情境物理 | 計時器、紙帶、點、秒數標號、台車的繪製；五個情境的 `x(t)` 與出界停止 | 手算對照表：五情境在 t = 0/2/5/10 的位置；斷言點高度 = `x(t) − x(tₙ)`；斷言所有點落在 [0, x(t)] |
| **T3** 三圖接上 | 8.6 的推導鏈、描點與打點同步、連線時機、讀數膠囊改 cm 單位 | 斷言 v-t 點的 t 座標落在時距中點；情境 4（等加速）的 a-t 點全部等於 0.8；三圖 `left`／`w` 一致到 0.5px 以內 |
| **T4** 手動拉紙 | 拖曳台車、位置單調不減、鉗在 [0, 40]、到底或到 10 s 自動停 | Pointer Events 模擬拖曳；斷言往回拖時位置不減少；斷言鬆手後時間繼續跑 |
| **T5** 量尺與兩點測距 | 可拖曳量尺＋0 點吸附；點選兩點 → 紙帶與 x-t 雙邊標 Δx | 斷言吸附後量尺 0 點與最近打點的畫面 y 相同；斷言兩邊標的 Δx 數值相同 |
| **T6** 收尾 | 四個顯示圖層在打點頁的行為、說明彈窗文案、移除佔位頁、`--u` 放大檢查、`DEV_SPEC` 第 8 節檢核清單 | 逐項回報 ✅/❌；四檔 `U`（1／1.15／1.9／2.3）下量紙帶三欄的相鄰間距 |

**開工前必讀**：第 3 節「放大機制的三個陷阱」（`--u` 與 `U` 的同步、canvas 座標一律走 `colX()`、
`--plot-left` 的每一項都要乘 `var(--u)`）。紙帶區是新的一組 canvas 座標，會完整踩到同一組坑。

### 8.8 本分頁刻意沒做（限制與解除條件）

| 項目 | 理由 | 要解除需要什麼 |
|---|---|---|
| 數據表（時距／點距／平均速度表格） | 使用者 2026-08-24 未勾選。「點選兩點測距」已涵蓋量與算的動作，表格會把答案直接送給學生 | 使用者要求加回 |
| 分段標記（每 N 點剪一段貼成階梯） | 同上未勾選。這個做法要另設一套剪貼視覺，成本高 | 使用者要求加回 |
| 60 Hz 真實頻率 | 10 秒 600 點、點距 0.06 cm，看不見也量不到 | 使用者接受「只能靠局部放大看」的呈現 |
| 手動拉紙不能倒退 | 真實紙帶無法倒著塞回計時器；且速度軸設計為單向 0–8 | 使用者要求可倒退（屆時速度軸要改對稱，v-t 面積要處理正負分塊） |
| 兩頁參數不連動 | 量級差兩個數量級（見 8.1） | 無——這是物理限制，不是取捨 |

### 8.9 接續開場 prompt（打點計時器專用，可直接貼進新 session）

```text
接續開發 motion-graphs-sim 的「打點計時器」分頁（國中理化直線運動模擬器的第二個分頁）。

專案目錄：C:\Users\Hope\Desktop\CLAUDE工作區\simulation\motion-graphs-sim\

動手前先完整讀過（路徑相對於專案目錄）：
1. PROJECT_PLAN.md 第 8 節 —— 打點計時器的完整規格與階段計畫。這是本次的權威來源，
   第 8.2 節的定案是使用者逐題裁示過的，不要重新徵詢。
2. PROJECT_PLAN.md 第 3 節「放大機制的三個陷阱」與第 4 節「技術決定」—— 紙帶區會完整踩到同一組坑。
3. index.html —— 現行程式碼（車子分頁已完成，打點頁目前是佔位頁 #screen1）。
4. ../_shared/DEV_SPEC.md —— 工程規範，第 0 節「鐵律」違反任一條即不合格。
5. ../_shared/design/DESIGN_SPEC.md —— 視覺規範。

特別注意：
- 8.5 節的紙帶模型（點跟著紙帶漂移、點高度 = x(t) − x(tₙ)）是最容易寫錯的地方，
  使用者已明確否決「固定紙帶」的簡化版，不要改回去。
- 8.6 節「三張圖全部由點距推出來、v 標在時距中點」是本分頁與車子分頁最大的內在差異。
- 三張圖的版面配置一律不動，只換左邊 222px 的內容。

先列出本階段的實作子步驟清單，經我確認後再動手。每個階段結束停下，附「怎麼驗證」的具體步驟。

本次要做的是：階段 T1（分頁骨架）
```

### 8.10 實作結果（2026-08-24，階段 T1～T6 一次完成）

使用者指定「直接把它做完」，六個階段一次交付。`index.html` 由 1615 行成長到 2497 行，
打點計時器分頁的程式碼集中在第 8 節（`TK_*` 常數、`tkS` state、`tk*` 函式），
與分頁 1 共用的只有：工具函式（`fmt`／`dashLine`／`px`／`roundRectPath`／`hexA`／`fitCanvas`／
`drawCar`／`ticks`／`refreshFill`）、放大係數 `U`、以及底部抽屜那一組播放控制。

抽屜的分派靠第 8.8 小節那七支 `cur*()`：`loop`／`play`／`syncPlayBtn`／三顆按鈕／scrubber
一律走 `cur*()`，由 `state.screen` 決定送給哪一頁。**分頁 1 的程式碼因此幾乎沒有被改動**，
只有那六處呼叫換成 `cur*()` 版本、`showScreen()` 補上切頁時的 fit 與 sync。

#### 機器驗收結果（Browser MCP，四種視窗尺寸）

| 項目 | 結果 |
|---|---|
| 三張圖左緣與寬度一致 | x-t／v-t／a-t 最大差 **0.01px**（要求 0.5px 內），1280×800、1080×810、810×1080、1920×1200＋`?u=1.9` 四種尺寸皆通過 |
| 紙帶區寬度 | 222 × U（U=1 時 222.00、U=1.15 時 255.29、U=1.9 時 421.79）全部符合 |
| `--plot-left` 加總 | U=1.9 時 91.2（縱軸）＋19（gap）＋421.79（紙帶）＋19 ＝ **550.99 ＝ 290×1.9**，放大陷阱 4 未復發 |
| `--u` 與 `U` 同步 | `?u=1.9` 下 CSS `--u` 與 JS `U` 皆為 1.9，放大陷阱 1 未復發 |
| canvas 與 CSS 欄位對齊 | 掃 canvas 像素，白色紙帶左右緣落在 `rt.x + 46U` ／ `rt.x + 176U`，誤差 1.8px（＝紙帶 1.5U 描邊寬度），放大陷阱 2／3 未復發 |
| 五個情境的位置 | t = 0／2／5／10 全部與手算相符（靜止 0；慢穩 25；快穩 40；慢而快 40；快而慢 40） |
| 這一輪的終點 | 五個情境的 `tkSimEnd()` 都是 10.0 s、`kind` 為 null（尺度剛好用滿，不會出現出界提示） |
| **v-t 標在時距中點** | 慢而快（a=0.8）的 10 個 v 點，t 座標為 0.5／1.5…9.5，值 0.4／1.2…7.6，**與該時刻的真實瞬時速度完全相等**（誤差 < 1e-9） |
| **a-t 由兩段速度差推得** | 9 個 a 點全部 ＝ 0.8，且標在整數秒（打點時刻）上 |
| 紙帶點的高度 | t = 6.5 s、x = 16.9 cm 時 7 個點高度為 16.9／16.5／15.3／13.3／10.5／6.9／2.5，**第一個點等於車的位置**、最後一個在打點頭、全部落在 [0, x(t)] 內；點距 0.4／1.2／2.0／2.8… 正是 1:3:5:7 |
| 推進到終點 | 手動推進 200 幀（rAF 在 Browser pane 不執行）到 t = 10，`drawn` 轉 true，繪製端無例外，讀數 10.0／10.0、位移合計 40.0 |
| 每次打點暫停 | 停在 1／2／3／4／5 秒（整數，非浮點殘值） |
| 打點頻率連動 | 2 Hz：單步 0.5 s、scrubber 19 刻度、時間軸 11 標籤、10 秒 21 點；0.5 Hz：單步 2 s、4 刻度、6 標籤、6 點 |
| 手動拉紙 | 拉到 10 cm 後往回拖仍是 10（單調不減）；超過上限鉗在 40；拉到底記下 `stopAt` |
| 量尺吸附 | 0 刻度吸到最近的打點（7.5 cm），誤差 < 1e-9 |
| 兩點測距 | 紙帶與 x-t 兩側標的是同一個 Δx；左欄同步顯示「點距 7.5 cm ÷ 時距 3.0 s ＝ 2.50 cm/s」，與慢穩情境的 2.5 cm/s 相符 |
| iPad 直向 810×1080 | 切換為上下堆疊、情境卡與圖層卡並排、抽屜兩列、scrubber 獨佔整寬，與分頁 1 行為一致 |
| **未破壞分頁 1** | 切回車子頁：scrubber 刻度回到每秒一格、抽屜換回「每秒自動暫停」、三圖對齊不變；車子跑完整一輪 200 幀到 t = 10（x = 200 m、v = 30 m/s）無例外 |
| 鐵律靜態檢查 | 無 `fetch`／`XMLHttpRequest`、無 `/` 開頭絕對路徑、無手寫 `<svg>` 圖示、無 Unicode 上下標、外部資源只有 FA CDN、字重只有 400／700、hover 全包在 `@media (hover:hover)`、新增元件視覺高度皆 44px |

#### 實作時自行判斷的四項（與 8.2／8.4 的字面規格有出入，使用者可否決）

| # | 實作 | 為什麼 |
|---|---|---|
| 1 | **預設情境是「慢穩」**，不是「靜止」 | 靜止情境的點全部疊在打點頭那一格、車也不動，一開機就看到那個畫面會被當成壞掉。5 個情境本身沒有預設值的規定 |
| 2 | **測距的數值不標在紙帶上**，紙帶側只畫雙箭頭；數值標在 x-t 那一側與左欄說明 | 130px 的紙帶上已經有秒數標號，再放一個數字方塊會直接疊在一起。8.2 原本寫「紙帶上標 Δx」 |
| 3 | **手動拉紙時，沒按播放就碰紙帶會自動開始計時** | 8.2 定的是「按播放後拖台車」。照字面做的話，學生一進來拖了沒反應，會以為壞掉；自動開始的結果與「按了播放再拖」完全相同，只是省一步 |
| 4 | **秒數標號只有第 0 點掛「秒」字**，其餘只標數字 | 每個點都掛單位會把 130px 的紙帶塞滿 |

#### 只能由使用者在真機驗證的項目（一律不得自報通過）

| 項目 | 已取得的替代證據 |
|---|---|
| 真的按下播放跑動畫 | Browser pane 的頁面 hidden、rAF 不執行（pitfalls-services 第 3 條）。已改用手動迴圈呼叫 `tkUpdate(dt)`＋`tkRender()` 推進 200 幀到 t = 10，驗證推進、每次打點暫停與繪製端無例外 |
| 三種觸控手勢（拖時間軸、手動拉紙、拖量尺） | 已直接呼叫 `tkPullTo()`／`tkSnapRuler()`／`tkPick()` 驗過邏輯與邊界，但 Pointer Events 在觸控裝置上的實際命中與捲動衝突量不到 |
| `file://` 雙擊直開 | 靜態證據：無 `fetch`／`XMLHttpRequest`、無絕對路徑、唯一外部資源是 FA CDN |
| 4× CPU throttling 的 fps | Browser pane 不合成畫面，量不到真 fps |
| 畫面截圖 | Browser pane 未顯示時截圖一律 5 秒逾時（既有限制）。改用 canvas 像素掃描：紙帶白 37038px、主色 4845px、墨點 1593px、量尺綠 2055px、機身灰 1130px＋描邊 962px，證明各元素確實畫出 |

#### 順帶發現、**本次未動**的既有問題

`iPad 直向（<900px）時，兩個分頁都是「設定在上、模擬在下」`，而第 3 節「2026-08-20 依設計稿定稿的修訂」
記載的稿 C 是「**模擬區固定在上**、車輛設定與顯示圖層並排在下」。兩頁行為一致（打點頁沿用了分頁 1 的
CSS 與 HTML 順序），所以這不是本次造成的偏差，而是分頁 1 既有的實作與記載不符。
要對齊記載的話，`.settings` 加 `order:2`（或把 stage-card 移到 HTML 前面）即可，**但這會同時改動分頁 1
的直向版面，屬於既有功能的變更，等使用者裁示再動**。

### 8.11 第二批需求（2026-08-24 使用者指定五項，取代 8.2／8.4 的對應內容）

使用者在第一批交付後提出五項變更，以下定案**取代上方 8.2／8.4 的對應列**。

| # | 需求 | 定案 | 取代了什麼 |
|---|---|---|---|
| 1 | 打點瞬間要看得出「真的有打」 | 打點頭改畫成**一根從機身伸出來的針**：打點瞬間針尖戳到紙面，`TK_STRIKE = 0.13 s` 內彈回原位（`tkStrike()`，用**模擬時間**算，所以慢動作時這一下也跟著變慢）；同時在剛打好的點上擴散一圈。開「每次打點暫停」時針會一直壓著紙 | 8.4 的「機身上朝紙帶壓下來的小三角」（靜態，不會動） |
| 2 | 移除公分量尺 | **整組移除**：核取方塊、拖曳手勢、`tkDrawRuler()`／`tkSnapRuler()`、`showRuler`／`rulerBase`、`TAPE_RULER_W` 全部拿掉。量尺原本佔的 46px 收回給紙帶，紙帶由 130 → **150px**（單條時） | 8.2「顯示圖層四項全做」的**公分量尺**那一項；8.4 的「量尺 46｜紙帶 130｜台車 46」三欄分法。**量點距的手段改為「點選兩點測距」＋「放大檢視」** |
| 3 | 情境與操作模式改下拉選單 | **自製下拉元件**（`ddHtml()`／`.dd`）：h-44、展開時邊框轉主色＋`0 0 0 3px` 光暈、選項清單白卡陰影、選中項主色淡底。**不用原生 `<select>`**——它的選項列高與選中樣式在 Windows 上控制不了 | 8.4「操作模式（情境選擇／手動拉紙）＋ 五個情境單選」的膠囊格（`.mode-chips`／`.scene-grid` 已刪除） |
| 4 | 紙帶要能放大並旋轉 | 新增**紙帶放大檢視**（`#tkZoomModal`，比照分頁 1 既有的軌道放大）：紙帶區右下角放大鏡開啟、開啟時自動暫停、倍率上限 6 倍。**旋轉為 90° 步進**，順時針／逆時針各一顆鈕，四個方向循環；轉 90° 後紙帶變水平，就是真實課堂把紙帶撕下來平放的樣子。旋轉用 `ctx.rotate()`，**文字另外反向轉回來**（`tkText()`），否則秒數會側躺 | 無（新功能） |
| 5 | 支援多條紙帶 | **上限 2 條**（使用者裁示，原本問的是 4 條）。`tkS.tapes[]`＋`tkS.focus`，比照分頁 1 的多車機制：眼睛鈕顯示／隱藏、垃圾桶刪除（至少留 1 條）、點卡片設為焦點。紙帶區 222px 依條數分欄（`TAPE_GEOM`：1 條 222／2 條各 111），三張圖畫多條曲線並在右端標字母。**切線／面積／參考線／測距一律只作用在焦點那條**。**手動拉紙只支援一條：切過去時直接清掉其他條**（使用者裁示），情境下拉與新增鈕一併禁用 | 8.2「只有一台車，左欄不要手風琴」；8.4 的單條版面 |

**2 條是關鍵決定**：4 條時每條只有 55px，秒數標號放不下，會被迫在「只標焦點」與「都不標」之間取捨；2 條時每條 111px，**秒數標號照常全標**，這個取捨消失了。

#### 實作時自行判斷的三項

| # | 實作 | 為什麼 |
|---|---|---|
| 1 | 下拉的**選項列高用 44px**，不是 `DESIGN_SPEC` 第 4 節寫的 42px | 列表項目的視覺尺寸就是它的命中區，42px 直接違反鐵律 6；而加高列表不會像加高按鈕那樣撐壞版面。**這是規範自身的矛盾**（元件尺寸 vs 鐵律 44），依「鐵律違反任一條即不合格」處理 |
| 2 | 新增第 2 條紙帶時，**自動挑一個與現有那條不同的情境**（優先序 慢而快→快穩→快而慢→慢穩→靜止） | 加出來如果兩條一模一樣，畫面上兩條紙帶完全重合，看不出「多一條」的意義 |
| 3 | 放大檢視是**唯讀**的：可以看、可以轉，但不能在裡面點選測距 | 旋轉後事件座標要反向旋轉才對得回打點序號，複雜度高；測距在主畫面做完再開放大檢視，標記照樣顯示 |

#### 機器驗收（Browser MCP）

| 項目 | 結果 |
|---|---|
| 針尖動畫數值 | 打點瞬間 1.00 → 0.06 s 時 0.65 → 0.13 s 時 0 → 下一個打點回到 1.00；像素掃描量到針尖實際位移 9px，且對準打點那一欄 |
| 量尺移除乾淨 | 核取方塊不存在、`showRuler` 不在 state、顯示圖層剩三個（與分頁 1 一致）、全檔「量尺／ruler」零命中 |
| 下拉選單 | 開合與 `aria-expanded` 正確、選取後自動關閉、選項列高 **44px**、按鈕高 44px；點下拉以外的地方會收起來 |
| 2 條紙帶欄位 | U=1.15：B 欄左緣 127.65 ＝ 111×1.15；U=1.9：210.90 ＝ 111×1.9，最後一條紙帶右緣都在 222×U 之內 |
| 兩條各自的物理 | t=6 時 A（慢而快）14.4 cm、B（快穩）24.0 cm，與手算相符；兩條曲線都畫進 x-t（像素掃描 A 572px／B 1198px） |
| 三圖對齊 | 兩條紙帶＋a-t 展開，U=1.9 下三圖左緣與寬度最大差 **0.01px** |
| 眼睛鈕 | 隱藏焦點那條時，焦點自動換到還看得見的那條 |
| 手動互斥 | 切手動後只剩 1 條、新增鈕禁用並顯示「手動拉紙只能一條」、情境下拉禁用；拉到 12 cm 後往回拖仍是 12（單調不減） |
| 放大與旋轉 | 開啟後 canvas 有內容；順時針 0→1（副標補「已轉成水平」）、逆時針兩次 1→0→3，四方向循環正確；U=1.9 下旋轉 90° 仍正常繪製 |
| iPad 直向 810×1080 | 上下堆疊、設定卡並排、紙帶卡 56px、下拉 322×44、刪除鈕沒有被擠出卡片 |
| 未破壞分頁 1 | 車子分頁跑完整一輪 200 幀到 t=10（x=200 m）無例外，scrubber 刻度切回每秒一格 |
| 鐵律靜態檢查 | 字重只有 400／700、canvas 字重全 700、hover 全包在 `@media (hover:hover)`、無 Unicode 上下標、無 `fetch`／絕對路徑／手寫 `<svg>`，外部資源只有 FA CDN |

**檔案現況**：`index.html` 2791 行（第一批交付時 2497 行）。

### 8.12 第三批需求（2026-08-25 使用者指定六項，取代 8.11 的對應內容）

| # | 需求 | 定案 | 取代了什麼 |
|---|---|---|---|
| 1 | 旋轉 icon 會被讀成「復原」 | 旋轉鈕改成**圖示＋「90°」文字**（`.zrot` 由 44×44 方鈕改為 auto 寬度、加框線）。角度本身變成標籤，弧形箭頭就不再被讀成 undo／redo | 8.11 第 4 項的純圖示鈕 |
| 2 | 操作模式要在最上面、且用膠囊 | 左欄卡片順序改為 **操作模式膠囊 → 紙帶清單 → 新增鈕 → 打點頻率 → 說明**。模式由下拉改回**膠囊**（`.mode-chips`，DESIGN_SPEC「2–4 個短選項→膠囊」），文案改成**自動拉紙／手動拉紙**。手動拉紙時紙帶固定一條、**新增鈕整顆收起來**（不是禁用）、情境下拉禁用 | 8.11 第 3 項把操作模式也做成下拉、以及它擺在卡片最下面的位置 |
| 3 | 打點頻率也要在左欄 | ~~**兩處並存**~~ **這一列已於 2026-08-25 作廢，見 8.13 第 2 項**。使用者當場否認曾同意「兩處並存」，定案改為**只有左欄一組**（`.freq-row`），抽屜那組整個移除，直向也顯示左欄那組 | 8.11 的「頻率只在抽屜」 |
| 4 | 手動拉紙要手開始拖才計時 | 新增 `tkS.started`：手動模式下**沒碰到台車以前 `play()` 不啟動**，播放與單步鈕都 disabled；第一次 `pointerdown` 才設 `started = true` 並開始跑。重置後回到等待狀態 | 8.11 的「沒按播放的話一併幫他按下去」——那個寫法會讓時間在車還沒動時空跑，前幾秒的點全疊在原地 |
| 5 | 每條紙帶要能各自選取兩點 | `selA`／`selB` 由全域**移進每條紙帶自己身上**（`tape.selA`／`tape.selB`）。兩條可以同時各量一段，紙帶與 x-t 圖上各用自己的顏色標出來；x-t 那側第二條的標註往右錯開 34px，數字才不會疊。左欄說明改成**每條一行**，行首用該條顏色的圓點。點某條的點**不再改變焦點**（焦點只由點卡片切換） | 8.11 的「測距只作用在焦點那條」「點到別條就把焦點換過去」 |
| 6 | 放大檢視也要能選點 | 放大檢視改為**可互動**：canvas 上的 `pointerdown` 經 `tkZoomInverse()` 反向旋轉回紙帶座標，再走與主畫面同一支 `tkHitDot()`／`tkSelectDot()`。測距標記也抽成共用的 `tkPaintMeasureOnTape()`，主畫面與放大檢視都畫 | 8.11 自行判斷第 3 項「放大檢視是唯讀的」——該項作廢 |

#### 連帶的版面調整

左欄多了模式膠囊與頻率列之後，內容在 1280×800 下超出卡片 1.2px、在 iPad 直向下超出更多。處理：

- **寬螢幕（≥900px）**：`.scene-card{overflow:hidden}` ＋ `.tape-list{flex:1;min-height:56px;overflow-y:auto}`，
  **只有紙帶清單捲動，控制項一律固定可見**；`.scene-note` 改 `flex:0 1 auto` 成為唯一可被壓縮的一列。
- **直向（<900px）**：維持整張卡可捲（那裡連固定列都放不下），並隱藏左欄的頻率列——抽屜那組仍在。
- 卡片內 `gap` 由 9px 收到 8px。

#### 一個判斷失誤與更正（**這段的結論已於 2026-08-25 被推翻，見 8.13 第 2 項**）

原始需求寫的是「打點頻率設定**也**做在左側的設定欄」，我第一次讀成「搬過去」，交付說明也照此宣告（移到左欄、抽屜不再放）。
實作後在 iPad 直向量到左欄放不下，重讀原文才發現「也」是**兩處都要**的意思。已改為兩處並存，這同時解決了直向的空間問題。

> **2026-08-25 更正**：使用者明確表示他從一開始就不要抽屜那一組，「兩處並存」是我自行加回去、未經同意的。
> 上面那段把「也」解讀成「兩處都要」是**第二次誤判**，且我把它寫成了定案紀錄，害它在後續 session 被當成使用者的指示沿用。
> 教訓：**自己改變過一次解讀的需求，不可以直接寫成「定案」**，要標成待確認並在下次交付時複述給使用者確認。

#### 機器驗收（Browser MCP）

| 項目 | 結果 |
|---|---|
| 左欄順序 | mode-chips → hr → 標題 → tape-list → 新增鈕 → hr → freq-row → 說明，模式確實在最上 |
| 模式膠囊 | 文案「自動拉紙／自動」正確、`aria-pressed` 同步；切手動後紙帶剩 1 條、新增鈕 `display:none`、標題顯示「手動拉紙・1 條」、情境下拉 disabled |
| 頻率兩組同步 | 點左欄的 0.5 Hz → 兩組 `aria-pressed` 一起變、單步變 2 s、scrubber 4 刻度、時間軸 6 標籤；點抽屜的 2 Hz → 同樣兩組一起變。直向時左欄那組隱藏、抽屜那組可見 |
| 手動計時 | 切手動後 `started=false`、播放與單步都 disabled；**按播放沒有任何反應**（running 仍 false、t 仍 0）；模擬手碰下去後 `started=true`、running 轉 true、播放鈕恢復、車被拉到 6 cm；重置後回到等待狀態 |
| 兩條各自測距 | A 選 [1,3] → 3.2 cm、B 選 [2,5] → 7.5 cm，兩者同時標在圖上；左欄說明兩行、行首圓點顏色分別是 `#2A6FDB` 與 `#2F9E63`；切換焦點後兩條的選取都保留 |
| 放大檢視選點 | 四個旋轉方向（0°／90°／180°／270°）下，`tkZoomInverse()` 的往返誤差都是 **0**，模擬點擊都正確選到指定的那個點 |
| 旋轉鈕 | 兩顆鈕都帶「90°」文字 |
| 左欄不溢出 | 1280×800、兩條紙帶下，**所有控制項都在卡片可視範圍內**，只有紙帶清單捲動；說明文字被壓縮到 32px 仍可讀 |
| 回歸（前兩批） | 物理（A 情境 x(10)=40／B x(10)=25）、v 標在時距中點且等於真值、a 恆 0.8、跑完 200 幀無例外、針尖 1→0.65→0、三圖對齊 0.01px、紙帶寬 222×U —— 全數維持 |
| 未破壞分頁 1 | 車子分頁跑完一輪到 t=10（x=200 m）無例外、scrubber 回到每秒一格、抽屜的頻率膠囊在車子分頁隱藏 |
| 鐵律靜態檢查 | 字重只有 400／700、canvas 字重全 700、無 `fetch`／絕對路徑／手寫 `<svg>`／Unicode 上下標，外部資源只有 FA CDN |

**檔案現況**：`index.html` 2876 行。

---

### 8.13 四方向審查與修正（2026-08-25）

四個 opus agent 分別就**功能正確性／認知負荷／平板與觸控可讀性／文案**審查打點分頁，
另派一個 agent 對照檢查車子分頁。使用者依審查結果指定修正如下。

#### 已修正（本批）

| # | 項目 | 內容 |
|---|---|---|
| A1 | 手動拉紙誤觸即結束 | `pointerdown` 原本不做命中判定，手指落在紙帶上緣就把車拉到 40 cm、`stopAt` 落在 t=0，整輪還沒開始就結束。新增 `tkOnCar()`：離台車 `TK_GRAB=40`（×`--u`）以內才算拖曳，否則走測距選點 |
| A2 | 時間軸沒受 `started` 閘門管制 | `syncPlayBtn()` 補上 `scrubber.disabled = waiting`。原本手還沒碰車就能把時間軸拉到 t=8，第一次拖車會把 0–8 秒的路徑整段回填成同一個位置 |
| A3 | 回看狀態下拖紙帶會瞬跳 | `tkOnCar()` 在 `t < pathMax/10` 時回傳 false。原本車不跟手（畫面查舊路徑表）但 `manualX` 已被改寫，續播越過該點會冒出一根假的速度柱 |
| A4 | 秒數標號跳號 | 原規則是「與上一個畫出來的標號距離不足 11px 就省略數字」，加速紙帶因此標成 `0 秒 3 4 5 6`（7 個點只有 5 個數字），會被讀成漏打一點——而這一頁要教的正是「時距固定」。改為 `tkSpreadLabels()`：把擠在一起的標號上下推開並拉細線指回它的點，真的推不下才整體退成等差的每 2／4 個標一次 |
| A5 | 測距標註被裁掉 | 選到靠近 t=10 的兩點時，x-t 側的雙箭頭與數字整組落在繪圖區外。`tkArrow()` 加 `side` 參數，右邊放不下就整組翻到左邊 |
| A6 | 超界的點靜默消失 | 手拉的 v／a 常超出 0–8、±1 的軸範圍，被 `clipRect` 吃掉，看起來像沒描點。`dot()` 加 `r` 參數，超界改在圖框邊緣畫指向外側的三角 |
| A7 | 每次打點暫停＋手動拉紙 | 時間停住但手還在拉，位移全灌進下一個時距讓平均速度虛高。`tkPullTo()` 內 `if (!running) play()` 把時間接回去 |
| A8 | 換螢幕後打點頁一直糊著 | `watchDpr` / `resize` / `pollScreen` 原本只叫分頁 1 的 `fitAll()`（它對 `screen!==0` early return）。新增 `fitCurrent()` 同時叫 `fitAll()`＋`tkFitAll()`＋`renderZoom()` |
| A9 | 重置語意與分頁 1 不一致 | `tkReset()` 原本整組 `tkInitState()`，紙帶條數／情境／模式／頻率全被清掉。改為保留這四項，只歸零時間與疊層 |
| A10 | 單步鈕 aria 沒還原 | `syncAll()` 補回「單步前進 1 秒」 |
| A11 | Escape 關不掉紙帶放大 | keydown handler 補 `if (tkZoomOpen()) tkCloseZoom()` |
| A12 | 手動取樣提前半格 | `tkRecord()` 的 `Math.round` 改 `Math.floor`（t=1.95 原本被記成 t=2.0 那一格） |
| B1 | **抽屜的打點頻率整組移除** | 使用者指定只留左欄那組（見上方 8.12#3 的更正）。連帶移除 `.freqbox` CSS、以及直向隱藏左欄那組的 media query |
| B2 | 單條紙帶不渲染眼睛鈕 | 唯一那條「暫時隱藏」沒有比較意義，卻是卡片上最搶眼的主色元素，按下去舞台全空 |
| B3 | **移除左欄兩段常駐說明** | `#tkSceneNote`（焦點紙帶的 v₀／a）與 `#tkMeasureNote`（測距算式）連同產生它們的 JS 一併刪除。使用者：「這些資訊對學生來講太多」 |
| B4 | 跑完後的收尾 | **第一版（紙帶續走 2.2 cm）當天即被使用者目視推翻，見下方「B4 的第一版失敗」。** 現行版本：新增 `tkS.tear`（0→1，`TK_TEAR_MS=520` 的 rAF），跑到終點時**計時器機身與針一起淡出消失**，紙帶在打點頭下方 `TK_TEAR_GAP=6`（×`--u`）處被撕斷，撕痕是單側鋸齒（`tkTearPts()`／`tkPaperPath()`，齒深 `TK_TEAR_DEPTH=2.5`），撕掉的那一截同步淡出。**點的位置一格都沒動**，所以 8.5 的「所有點落在 0～x(t) 之間永不溢出」不變式完好，參考線也不必關掉。收尾後最後一點不再放大（3.5 而非 4.5），那 1px 剛好是它與撕痕之間的空隙 |
| B5 | 測距改標在紙帶與圖上、新增速度讀取 | 選兩點後 **v-t 圖上標出這段平均速度所在的點與數值**（選第 7、8 秒就標 7.5 秒那個點——量到的與圖上畫的是同一個數）；**v-t 圖可以直接點任一個點**讀出它的時刻與速度（命中半徑 `22×U`，比紙帶的 15 大，手指點得到），再點一次收起。新增 `tkValueTag()` 統一畫圈＋膠囊並自動翻邊 |
| B6 | 用語統一 | 「打點間隔」→**時距**；放大檢視副標「相鄰兩點相隔 X 秒」→「時距 X 秒」、「量距離」→「量點距」；說明彈窗「相鄰兩點的距離」→「點距」；`手動拉紙・1 條` 的日文中點 U+30FB → `·` U+00B7 |
| — | 手動模式的操作提示 | B3 拿掉「拖動台車就會開始計時」後，配合 A1 只准抓台車，學生沒有線索。改在紙帶 canvas 上、車的正上方畫一個往上的箭頭（`cue` 參數，只在主畫面畫，放大檢視不畫），開始拖之後消失 |

#### 機器驗收（Browser MCP，1280×800，U=1.15）

| 項目 | 結果 |
|---|---|
| A1 | 點紙帶上緣：`started=false`、`stopAt=null`、`t=0`、`manualX=0`；抓在台車上：`started=true` |
| A2 | 切手動後 scrubber 與播放鈕皆 `disabled`；開始拖之後兩者解鎖 |
| A3 | 回看時 `tkOnCar()` 回 false；在該狀態下 dispatch pointerdown 後 `manualX` 與 `pathMax` 均未改變 |
| A4 | 慢而快 1 Hz、t=6.4（7 個點）：舊規則只標 `0,3,4,5,6`，新規則標 `0,1,2,3,4,5,6` 全出；最小間距 11.0px＝門檻，最大位移 11.8px，全部落在紙帶內 |
| A5 | x-t 繪圖區寬 538px：選 2–3（右緣剩 377px）掛右、選 6–7（剩 161px）掛右、選 9–10（剩 0px）翻左 |
| A6 | 手拉出 30 cm/s（軸上限 8.4）：v-t 上緣該處著色 118 px，超界標記有畫出來 |
| A7 | `pauseOnTick` 停住後 `running=false`，呼叫 `tkPullTo()` 後 `running=true` |
| A8 | 停在打點頁把 `tkFgCanvas.width` 破壞成 10，`fitCurrent()` 後復原為 1337 |
| A9 | 2 條紙帶、情境 `[4,2]`、freq 2、t=4 → 重置後條數／情境／freq 全部保留，只有 t 歸 0 |
| A10 | 打點頁「前進一個時距」／車子頁「單步前進 1 秒」，切頁正確互換 |
| A11 | `tkOpenZoom()` 後 dispatch Escape → `tkZoomOpen()` 轉 false |
| A12 | t=1.95 呼叫 `tkRecord()` → `pathMax=19`（對應 1.9 s），不再被記成 2.0 |
| B1–B3 | `#tkSceneNote`／`#tkMeasureNote` 皆不存在；`.drawer .tk-freq` 0 組、`#screen1 .freq-row .tk-freq` 1 組且 `display:flex`；單條紙帶時 `.tape-eye` 0 顆 |
| B4 | **四個情境（慢穩／快穩／慢而快／快而慢）收尾後都沒有任何點跑出紙帶區**：跑滿 40 cm 時最前點距紙帶頂 10.0px（＞點半徑 3.5px）。撕痕基準線 245.1、最後一點下緣 242.6 → 間隙 2.5px 不撕到點；齒最深 247.6 ≤ 紙帶底 249.1 不超出。tear 由 0 動到 1、`tkStrike()=0`、撕痕帶著色 1054px；**機身左右突出段著色 0（對照組未收尾時 405／162 深色）＝ 計時器確實消失**；往回捲到 t=6 → tear 歸 0、針回 1；`x-t` 最後一點仍為 40.00。2 條紙帶＋2 Hz（各 21 點）收尾後兩條都沒有點跑出去 |
| B5 | 選第 7、8 秒：量到 6.000 cm/s ＝ `tkSegV(tape,8)` 6.000，中點 7.5 s；v-t 中點圈著色 522px、數值膠囊 1175px、x-t 選取圈 559px；點 v-t 上第 8 段選到 `{id:'A',n:8}` 顯示「7.5 s / 6.0 cm/s」，再點一次歸 null |
| 回歸 | 車子分頁跑完 200 幀到 t=10 無例外；打點分頁 2 條紙帶＋2 Hz＋三圖層全開＋a-t 展開跑完 200 幀無例外，跑完後測距與讀值亦無例外；A（慢而快）與 B（快而慢）x(10) 皆為 40.0 |
| Console | 全程 0 錯誤 |

#### B4 的第一版失敗（2026-08-25 當天被目視推翻）

第一版把收尾做成「紙帶再往前滑 2.2 cm」。使用者實測後回報：**跑完後最前面 1～2 個點會跑出範圍外**。

原因是我破壞了 8.5 的不變式。位置軸是 −2 ～ 42 cm，而所有點的高度都是 `x(t) − x(t_k)`，
落在 0～x(t) 之間；因為 `x(t) ≤ 40 < 42`，**上方永遠留著 2 cm 餘裕**，點才不會溢出。
滑行把 `xNow` 加了 2.2 cm，剛好吃光那 2 cm 還超出 0.2 cm：

| 情境 | x(10) | 上方餘裕 | 滑行後第 0 點 | 結果 |
|---|---|---|---|---|
| 慢穩 | 25 cm | 17 cm | 27.2 cm | 沒事 |
| 快穩／快而慢 | 40 cm | **2 cm** | 42.2 cm | 第 0 點在紙帶頂外 1.2px |
| 慢而快 | 40 cm | **2 cm** | 42.2 cm | 第 0 點在外，**第 1 點只距頂 1.2px**（點半徑 3.5px，被裁掉大半） |

使用者裁示：取消滑行，改成**計時器淡出消失 ＋ 紙帶在原地被撕斷（但不要撕到點）**。
新作法完全不動點的位置，不變式因此完好無損，連帶也把第一版為了遮掩偏差而關掉的參考線恢復了。

> **教訓**：`TK_POS_HI = 42` 與 `TK_X_LIMIT = 40` 之間那 2 cm 不是留白，是**不變式的安全邊界**。
> 任何改變紙帶繪製位置的功能都會吃掉它。要動紙帶的視覺，優先選「不移動點」的作法。

#### 移除抽屜頻率組的連帶處理（直向）

抽屜那組拿掉之後，直向（<900px）只剩左欄一組，但那裡的設定卡原本只有 ~138px 高、整張可捲，
而頻率列排在卡片最下面——等於**直向要捲到底才點得到打點頻率**。處理：

- `#screen1 .freq-row` 在直向改 `position:sticky; bottom:0`，永遠釘在設定卡底部，上緣加陰影。
- 釘住之後可視區再少 56px，紙帶清單會整個掉到摺線下 → `#screen1 .stage-card` 在直向由 560 改 **470px**，
  設定卡因此由 138 → **228px**。**只改 `#screen1`，車子分頁的直向舞台維持 560px。**
- 代價：直向的打點分頁繪圖區變矮（實測紙帶 220、x-t 220、v-t 101px，v-t 剛好卡在 `min-height:100`）。
  **這是為了讓四組控制項在直向都碰得到而付的代價，若覺得圖太矮可以回頭調這個數字。**

#### 本批**未**處理（審查有提出，等使用者裁示）

1. **`--u` 判定在兩種目標裝置上等於沒生效**：`uiScale()` 用 `screen.width × DPR` 分級，門檻 2000／3000。平板直向（810×1080 DPR2 → 1620）與 **1920×1080 DPR1 的教室大屏（→1920）都判成桌機 U=1**；`clampScale()` 的 `cssW/900` 又讓 768 寬的直向永遠被鉗回 1。全檔 41 處字級 < 14px（紙帶秒數 10px、面積格數值 10px、內側縱軸 11px、讀數膠囊 12px）因此在大螢幕上等比例偏小。
2. **兩條曲線在播放全程沒有字母**：`tag()` 對焦點那條直接 return，且只在 `done` 時呼叫 → 整段播放只靠藍綠色相（互比 1.41:1）與線寬 3px/2px 區分。分頁 1 的四台車同構且更嚴重（四色兩兩對比 1.06–1.49）。
3. **白紙帶對桌面底 1.14:1**：投影後紙帶會消失，只剩一串黑點浮著。
4. **觸控目標**：`.track-zoom` 被 `overflow:hidden` 裁成 41×41（兩個分頁同構）；scrubber 把手水平命中 26px；紙帶上 1 Hz 的點距只有 13.1px、2 Hz 6.6px，手指指不準（要靠放大檢視）。
5. **`.tapecard`／`.car-head` 是 `<div>`**，`button:active{opacity:.55}` 選不到，觸控裝置上點下去零回饋；`touch-action:manipulation` 也未涵蓋它們（iOS 雙擊會縮放）。
6. **U ≥ 1.28 時固定 px 盒溢出**（`.tapecard` 56px 內含 `calc(44px*u)` 子元素）；分頁 1 的 `.car-head` 門檻更低（U > 1.00）。
7. **平板直向左欄只剩約 140–150px**：情境下拉展開需 228px 會被容器裁掉；分頁 1 的 `.cars-list` 只剩 35px、`.layers-card` 溢出 44.7px。
8. **共用規範自身矛盾**（`_shared/`）：下拉列高 42 vs 鐵律 44；「元件不乘 `--u`」vs「字級乘 `--u`」；字級下限 12 vs 14；色票表仍列已不合格的 `#7A8496`／`#2F9E63`；`DEV_SPEC 5.2` 示範碼類名 `.scrollable` 與實作 `.cars-list` 對不上。
9. **說明彈窗仍保留「瞬時速度／切線／差半格」那段**（文案審查認為超綱）——8.6 明文要求要講，未經裁示不動。

**檔案現況**：`index.html` 3089 行（8.12 交付時 2876 行）。
